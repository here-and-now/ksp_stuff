# Lessons

RO science sandbox (`KSP-RO`). Gym pad/hop tape is
`docs/archive/letsgrok-2026-08-21/`. Kerbin/Mun:
`docs/archive/kerbin-lessons.md`. kRPC 0.6 facts that are still true:
`docs/agent-notes.md`.

After anything unexpected (failed API, wreck, empty HD, EC=0):

1. Append a heading **`## <run> — title`** (Earth UTC filename stem).
   No letter-codes.
2. Put the fix in a `.py` next to `main.py`.
3. Patch `docs/agent-notes.md` if the API fact is still current.

No runs yet.
