# Science card — jebediah

science: none
recover_banks: n/a

Bind after `capable: yes`. Each experiment needs `experiment_id`,
`part`, instrument + tech + unlocked (`f013`), `duration_s`,
`ec_rate`, `recover_banks: yes|no`.
