# Slate

**Goal:** Kardashev III or bust. Moon is a waypoint. Probes first.
**Today (Os 2026-08-24):** bigger rockets, more Δv, farther out. Ad
astra. Not a 15-sci stunt. Bank **8.7721** stays in the drawer until
`stability` **18** pays (~**9.23**). Farther hops that can bank
FlyingHigh / a new biome are the work. Tape is the product. Save
`letsgrok` / `~/Games/KSP-rss`. CTT+PBC. RO sandbox is the next tree,
not this save.

**Bank:** desk **sci 8.7721**. Tree **start,engineering101,basicRocketry,survivability**. `stability` (18) LOCKED — need ~**9.23**. `load persistent` is forbidden (F-014).
**Aero (2026-08-21):** FAR, RealChute, RealHeat **installed**. Kerbalism
Default. Not RO. Chute parts **unlocked**. No FAR kRPC in this repo.

**Next:** Gus **T-318** hangs more Δv than stiff-pbc (~30 km apo, Forest
17–19 km, living recover, **sci +0** on 21-46-20Z / 21-53-04Z). FlyingHigh
Forest leftover **1.51** waits **≥50 km**. New biome waits envelope. Forest
SrfLanded leftover waits a stack that actually **starts** landed thermo —
do not light stiff-pbc for that bind again. Next CTT is still `stability`
**18** when the bank pays. `campaign: uncrewed`.

**How today:** pad occupancy on a hang that can **pay**, not another +0
Forest loft. Idle pad is a sin; a living recover that cannot pay is
waste. Bind last-envelope biome/sit the **new** hang can reach
(side-by-side). Warp the coast. Thin tape → `type=systems`. Do not
recover Ast. XRL-564. Do not idle the pad for a letter or a 15 min
replan.

Canonical save `letsgrok`. `KSP-RO` is parked. Honor 4761 lives in
`docs/archive/` only.
