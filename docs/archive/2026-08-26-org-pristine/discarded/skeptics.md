

===== SKEPTIC 0 =====
**real=false.** The letter archives a second bus and unpins tests. It does not ticket CHARTER/portraits/jsonl/`desk.md`, does not drop `protocol fly` plan fallback this release, does not restore a second kRPC writer, does not idle the pad for a novel, and does not delete Learn/bind tape.

**Fail gates (none trip).** KEEP-MD keeps creed, seated `plan.md`, and fly fallback one release (`OPS.md:6-8`; `protocol.py:132-135` `ff.get("go") or plan.get("go")`). Seq 4 kills `sync_shim` (`missions.py:192-209`), not that gate. One writer stays (`AGENTS.md:111`; NUKE is retired `spotter.md:11-13`). Pad stays hot (`CHARTER.md:164-165`; seq 1–6 “Pad flies”; pulse not this slice). Learn is still hop-exit `attach-run` (`PROTOCOL.md:351-353`; `BRIEF.md:10-12`). Bind is science payload (`PROTOCOL.md:526-530`). Pad already tickets-first, then seated `science.md` (`pad.py:65-81`) — seated card is not in ARCHIVE. Jsonl schema untouched.

**Poison on disk (second bus).** Packet skim is already `desk.md`+`BRIEF.md` (`tickets.py:396-397`) plus fly/science/vab/blocks novels (`:405-420`). Parent still injects CHARTER+last-flight+lessons (`AGENTS.md:3-9`, `:326`, `:345`). Twins: `migrate_second_bus` (`tickets.py:1632-1692`); tests pin `Forest is 270` and `## ` (`test_protocol.py:145-147`, `:197-200`). `desk.py:624` clips `note-tech`. Uncrewed tape hardcodes `jebediah` (`tickets.py:406,:424,:458`; `house_dump.py:20-22`) while `current.md:1` seats jebediah; CHARTER already `docs/missions/<id>/logs/` (`:163-164`); `GLOSSARY.md:10` is the stale word. `lessons.md` ends `:2872`; `world-model.md` ends `:800`. `ID_PREFIX` S/M/C (`tickets.py:59-63`) and `hop_factory_pad.py:1-8` stay. T-376 `done` (`head.json:13664-13681`). Lars compose is **after** 1–7 (`hop_factory.py:1051` still the immortal timeout). Floor clash: `PROTOCOL.md:349` Lars **low** vs `AGENTS.md:137` **medium**.

**NUKE/ARCHIVE targets exist.** `.grok/agents/spotter.md`; `sit-card.json` (`GLOSSARY.md:37` retired); `tickets/BOARD.md`; `docs/crew/builder.md`; `lars-rsi.md`/`learn-rsi.md`/`feedback-plan.md`/`RO.md`/`org-flow/`. Cards already half-cut (`linus.md:16` dump; `gene.md:19-20` no world-model dispatch). Do not land this letter as live `docs/program/*.md`.

**Crew logs (voice tape, not dispatch).** `^- ` ≥295 across `docs/crew/log/` (capped). Last 40 are newest-first: `lars.md:1-40` rf-ignition/pad-hold; `mortimer.md:1-40` lars-rsi applied, pad still flies; `wernher.md:1-40` T-467 prefix / T-449 eyes; `hank.md:1-16` 09-01Z postcard. Jeb log is hop-exit tape (`jebediah.md:9+`).

===== SKEPTIC 1 =====
**real=true.** The DESIGN archives RSI novels, deletes the spotter card, and drops BOARD from packet skim. A cold hire is still pointed at `docs/lessons.md` as miss physics, still given a `jebediah/logs` attach-run recipe, and still has `note-tech` as a live desk-to-desk mailbox. Fail closed: those writers are unnamed in the sequence.

## What the DESIGN actually kills

- Spotter: NUKE `.grok/agents/spotter.md` (`AGENTS.md:106`). No test pins it. After slice 5 it is not a spawnable card.
- BOARD skim: `infer_links` never adds `BOARD.md` (`tickets.py:396-397`; tests `assertNotIn`). Slice 5 deletes the file. Recreate via `PRINT` (`tickets.py:16,:744`) is Hank `tickets board`, not packet `read:`.
- RSI novels: slice 2 git-mv `lars-rsi.md` / `learn-rsi.md` / `feedback-plan.md` under `docs/archive/` (today they classify as `live_kernel`, `docs_inventory.py:345`).

## What a cold hire still gets

**lessons.md as miss physics.** Slice 3 cuts `AGENTS.md:3-9,:326`, `PROTOCOL.md:564`, `lars.md:165`, `wernher.md:92`, BRIEF `:83`. It does not edit KEEP `OPS.md`. Cold Hank’s kernel is still `OPS.md` (`.grok/agents/hank.md:78`); data-flow still says `lessons.md ← VSE/CSE dated physics/API` (`OPS.md:369`). NUKE stubs `docs/lessons.md` in place, so the path stays live. Portrait `docs/crew/lars.md:33` still orders a lessons heading (not in the 8-card cut). `FORBIDDEN_DISPATCH` (`docs_inventory.py:27-32`) never lists `docs/lessons.md`.

**uncrewed attach-run still `missions/jebediah/logs`.** Slice 7 names only `tickets.py:406,:424,:458` and `house_dump.py:20-22`. Hop-exit `attach_run` takes `log_path()` (`main.py:99-106`) from `seated_logs_dir(seated_id())` (`flightlog.py:246-249`, `missions.py:103-104`). `current.md:1` is still `flight: jebediah`. CHARTER keeps `docs/missions/<id>/logs/` (`CHARTER.md:163-164`) and creed is do-not-touch. Packet how-to stays hardcoded: `BRIEF.md:63`, `hank.md:122`, `OPS.md:100`, `tickets/README.md:26`, `docs/flights/README.md:15`. No later ticket owns the writer.

**note-tech stays dispatch.** Slice 6 only stops the desk clip (`desk.py:624`). Mailbox law stays `Commander → Lars/Gus/Wernher` (`uplink.py:8-9`, `main.py:755`). KEEP `GLOSSARY.md:31` still “Commander may still append.” `pilot.md:64` only relabels tape. Ticket 5 is the clip fingerprint, not the CLI.

Tickets-to-open miss slice 7 (writer dest) and the note-tech writer. Spotter/BOARD ORs are false after the DESIGN; the other three are not.

===== SKEPTIC 2 =====
**real=false.** The DESIGN kills the second bus; it does not keep Lars `lessons.md` append, `need_*`/Return keys, world-model Open questions as ask, or a new live `docs/program/*.md` as the deliverable. Disk still has that poison; this letter is the cut list, not another novel.

## Four gates (fail closed)

- **Lessons append:** poison is live (`lars.md:165`, `wernher.md:92`, `AGENTS.md:326,:345`, `PROTOCOL.md:564`, `OPS.md:369`). DESIGN stops it, archives `docs/lessons.md` (2872, **156** `##`), stubs “do not append,” unpin `test_protocol.py:145-147,:197-200` (`Forest is 270` at `lessons.md:1233,:1248`). Next miss = helper docstring + `finding:`/`close_why`.
- **need_*/Return:** `NEED_MAP` still `tickets.py:121-130` (shim). DESIGN: no emit, no restore. Ask → `ops --tag ask`.
- **Open questions:** live table `world-model.md:768`. DESIGN archives the dump, 20-line chair stub, ask on tickets. `gene.md:20` already says not to dispatch there.
- **New org novel:** letter forbids live `docs/program/*.md`. ARCHIVE `lars-rsi`/`learn-rsi`/`feedback-plan`/`RO`/`org-flow`. KEEP-MD is existing kernel. Later work is **tickets**, not a page.

## Cite nits (not a gate)

- Skim is `tickets.py:396-397` **plus** briefing/science/vab/blocks `:405-420` — seq 4 drops those.
- Packet findings `:530-546`; `close_why` is `:1660`. Twins `:1666-1692`.
- `classify` starts `docs_inventory.py:307`; `:345` is `live_kernel`.
- T-376 id `head.json:13673`. Factory still ends `hop_factory.py:1051`.
- `PROTOCOL.md:349` Lars **low** vs `AGENTS.md:137` **medium** — DESIGN fixes the floor.
- Uncrewed tape hardcoded `jebediah` (`tickets.py:406,:424,:458`, `house_dump.py:20-22`, `GLOSSARY.md:10`); CHARTER is `missions/<id>/logs/` (`CHARTER.md:163-164`).

## Disk (DESIGN sequence, not this turn)

NUKE still live: `.grok/agents/spotter.md` (`AGENTS.md:106`), `sit-card.json` (`GLOSSARY.md:37`), `tickets/BOARD.md`, `docs/crew/builder.md`, `missions.py:192` `sync_shim`, `desk.py:624` note-tech. Parent inject `AGENTS.md:3-9`. Pad-RF docstring `hop_factory_pad.py:1-8`. `docs/archive/` is **PARKED**. Crew logs = TAPE (lars last: 2026-08-25 T-469); not dispatch. Slice 8 compose stays later.

===== SKEPTIC 3 =====
**real=false**

The letter has a first-slice stop-append path and a later Lars compose ticket from Wernher blocks. It keeps `hop_factory.py` this paper; it does not tell Lars to keep `hop.py` as the factory.

## (1) Stop appending `lessons.md` — present

Sequence 1 + NUKE stub + job-card cuts. Disk matches: append still live at [`.grok/agents/lars.md:165`](.grok/agents/lars.md), [`.grok/agents/wernher.md:92`](.grok/agents/wernher.md), [`AGENTS.md:326`](AGENTS.md), [`PROTOCOL.md:564`](docs/program/PROTOCOL.md). Twins at [`tickets.py:1666`](tickets.py)–`:1692`. Tests pin the novel at [`tests/test_protocol.py:145`](tests/test_protocol.py)–`:148` (`Forest is 270`) and `:197`–`:200` (`## `). `docs/lessons.md` ends `:2872`. Cite `docs/crew/lars.md:165` is **MISSING** (file is 57 lines); the job card is the real line.

## (2) Living-rocket compose — present, later

Ticket 3 / sequence 8: `type=control` desk=lars fingerprint `control-blocks`. Allowed by the brief. Law already [`PROTOCOL.md:139`](docs/program/PROTOCOL.md)–`:145`, [`OPS.md:26`](docs/program/OPS.md). T-376 exists [`head.json:13664`](docs/program/tickets/head.json); fingerprint count 7. Catalog named as `physics_warp.py` + sit predicates; `blocks.md` still a novel [`docs/program/blocks.md:1`](docs/program/blocks.md). Pulse not retuned this paper.

## `hop.py` factory — not in this letter

[`hop.py:1`](hop.py)–`:3` is parked water/splash + shared helpers. Inland is [`hop_factory.py:1051`](hop_factory.py) (`run_factory_vessel`). Pad-RF stays [`hop_factory_pad.py:1`](hop_factory_pad.py). DESIGN parks that; it does not restore immortal `hop.py`.

## Logs

`docs/crew/log/lars.md`: `lessons` **0**, `compose` **0**, `hop.py` ≥4 (old T-326…), `hop_factory` ≥4 (T-469… newest). Last 40 start at `:3` T-469 RF pad; T-335 control-blocks is `:25`.

===== SKEPTIC 4 =====
**real=false.** After 1–7, Hank still runs leftover → `desk` → `attach-run` → `landing` → `ops next`. Kernel already does not read BOARD/lessons/note-tech/world-model/dual plan for that loop. Children already address desks by ticket (`ops --tag ask` / `payload.to`), not only MD. Second bus is spawn poison; pad stays hot.

## Ops next (today)

- Hire graph: `ops.py:147–379` (`list_tickets` + `parse_desk`). Leftover: `ops.py:121–134` hangar/n/overlay. Fly gate: `ops.py:419` tickets, not plan.
- Packet: `tickets.py:396–397` desk+BRIEF; extras still `406–420` (jebediah briefing, science.md, vab.md, blocks.md) — seq 4.
- Findings skim: `tickets.py:530–546`. Inbox ask: `tickets.py:1406–1425`. PROTOCOL `90–93`, BRIEF `26–27`.
- BOARD.md is a dump, not input: `_rebuild` `tickets.py:303` + `_write_board_md` `719`. `ops next` uses `head.json`.

## Inject / twins (seq 1–3)

- Parent stuffing: `AGENTS.md:3–9` (CHARTER, PROTOCOL, last-flight, lessons).
- Lessons pin: `tests/test_protocol.py:145–147`, `197–200`. Heading twins: `tickets.py:1666–1692`. lessons.md has **156** `##` (~2872 lines).
- Lars floor: `PROTOCOL.md:349` low vs `AGENTS.md:137` / `lars.md:14` medium; `hank.md:54` still **low**.
- Cards still append lessons: `lars.md:165`, `wernher.md:92`. Spotter live: `.grok/agents/spotter.md`, table `AGENTS.md:106`.

## Dual plan / tape

- KEEP fallback: `protocol.py:132–135` plan kv. Shim writers: `missions.py:192–209`, also `main.py:922`, `uplink.py:211`.
- note-tech clip: `desk.py:624` (test `test_desk.py:281–285`). TAPE classify: `docs_inventory.py:343`.
- Uncrewed path hardcodes jebediah: `tickets.py:406,:424,:458`, `house_dump.py:20–22`; seat `current.md:1`. CHARTER already `docs/missions/<id>/logs/` (`CHARTER.md:163`).
- `docs_inventory.py:345` default is `live_kernel` — `lars-rsi.md` / `learn-rsi.md` / `world-model.md` (~800+) stay kernel until git-mv.

## Gaps (do not flip)

- Seq 5 NUKE `BOARD.md` without killing `_write_board_md` — file respawns; Hank still need not open it.
- `hop.py`/`pad.py` still parse seated `science.md` — later ticket 4, not ops next.
- Compose stays later (`hop_factory.py:1051`; pad-RF `hop_factory_pad.py:1–8`).

## Crew logs (counts, not dump)

`lessons.md` **0**, `BOARD.md` **0**, `world-model` **1** (`docs/crew/log/mortimer.md:53`), `note-tech` **1** (`jebediah.md:3`), `ask`/`payload.to` **0**. Hank log is 14 bullets; last is 2026-08-22 T-013 re-fly (`hank.md`).

Do not apply this letter as live `docs/program/*.md`. Open the six tickets later; do not retune pulse.