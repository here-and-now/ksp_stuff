# PARKED — comms-org-rsi applied (2026-08-26)

Not dispatch. Not work. Ticket bus is `python main.py tickets`.
This file is the apply closeout. Do not spawn-read it.

`do_apply=true` `overnuke=false` `skeptics_ok=true` `stamp=2026-08-26-comms`
`archive_dir=docs/archive/2026-08-26-comms-org-rsi` (not a live `docs/program/` novel).
HEAD at closeout: `c11e1c3` (`org-rsi verify: open deferred systems/control tickets; T-471 untouched`).
No live `docs/program/org-rsi.md`. No Hangar. No hop. No pulse retune. No CHARTER creed rewrite. No slate goal rewrite.

Prior bibliography **PARKED** `docs/archive/2026-08-26-org-rsi/` (opened `README.md` first, then `APPLIED.md`). Disk wins. Discarded letters are not law.

Keep unless a later ticket proves otherwise: learn-rsi `attach_run` overwrite of uncrewed `payload.learn` (`main.py:106`; `tickets.py:1464`); lars-rsi `ID_PREFIX` S/M/C (`tickets.py:58–62`) + `hop_factory_pad.py` as the pad-RF file (22 `def`, ends `:557`). Do not restore spotter as spawnable, Gene-as-merge, Batch Learn, Return keys as the bus, or `need_*` as hire tokens.

---

## 1 Sit

Live sit object (`docs/program/desk.md:1–16`, gitignored snapshot; **not** rewritten this closeout):

| kv | live |
|---|---|
| `lock` | `free` (`desk.md:1`) |
| leftover | **0** (`desk.md:5`) |
| seat | `uncrewed` (`desk.md:6`) |
| sci | **13.6243** (`desk.md:7`) |
| tree | `start,engineering101,basicRocketry,survivability,stability` (`desk.md:10`) |
| `capable` | `yes` (`desk.md:11`) |
| craft | `kspstuff-hop-valiant-t7-wheel-proc-hs-cone-pbc` (`desk.md:12`) |
| `card` | `barometerScan,mysteryGoo,geigerCounter` (`desk.md:14`) |
| last | `command=hop exit=0 abort=none` (`desk.md:15`) — postcard `docs/last-flight.md:1–4` is `exit: 2` `abort: pad-dead-no-plume` |
| bind | T-404 / T-460 / T-461 PresMat (`desk.md:26`) |

Tape id vs Commander (`docs/program/current.md:1–3`): `flight: uncrewed` / `pilot: none` / `capcom: Walt Grokman`. INDEX lists tape id `uncrewed` vs Commander dossier `jebediah` (`docs/missions/INDEX.md:3–10`). Seated plan is envelope only (`docs/missions/uncrewed/plan.md:1–13` `hop_apo` / `expect_*` / `emergencies`; no `go` / `cli` / `campaign`). Jebediah plan is historical dossier (`docs/missions/jebediah/plan.md:1–13`).

Radio (`docs/program/ship.md:5–8`): `as_of: 2026-08-26T12:19Z` `sit: pre_launch` `flight: uncrewed` vessel `…t7-wheel-proc-hs-cone-pbc`. Uplink cleared (`docs/program/uplink.md:1`).

Fly ticket T-081 still `go: yes` / `campaign: uncrewed` / `cli: python main.py hop` (`head.json:3416–3420`). `payload.learn` is the 12-18-51Z envelope (`head.json:3642`); `telem_run` `docs/missions/uncrewed/logs/2026-08-26T12-18-51Z-hop.jsonl` (`head.json:3649`). Hangar name is C-477 `capable: yes` + `payload.craft` `…t7-wheel-proc-hs-cone-pbc` (`head.json:215–225`; still `inbox` with recover tag `:268–273`). T-400 is **not** the live hang. Live pad miss is **T-471** `type=control` `desk=lars` `fingerprint=rf-ignition-ullage` S1 P1 `inbox` (`head.json:18299–18370`). T-471 is pad-RF, not compose.

Goal unchanged: bigger rockets, more Δv, farther out. Ad astra. Next CTT `generalRocketry` **20**; bank line on slate is stale vs desk **13.6243** (`docs/program/slate.md:3–11`; `CHARTER.md:3–11`). Do not spend crumbs. Idle pad is a sin (`CHARTER.md:29`).

Git apply (`.git/logs/HEAD:124–129`; `refs/heads/main` = `c11e1c3`):

| commit | slice | title |
|---|---|---|
| `983cf46` | 1 | Unpin tests before leftover-writer and dump cuts |
| `b992cda` | 2 | Kill leftover writers, inventory holes, dual-board dump |
| `89949aa` | 3 | Cheap-cut job cards: packet first, drop sit novels |
| `022278e` | 4 | Switchboard: lock-live TUI vs ops next; hop light; dead links |
| `4c1654c` | 5 | Sit identity: capcom Walt; Commander dossier not live hop |
| `c11e1c3` | verify | open deferred systems/control tickets; T-471 untouched |

This wave sits **after** the 2026-08-26-org-rsi park (`6254d492` and siblings) and **after** post-apply pad occupancy (T-471 pad-RF growth, C-477, 16 uncrewed hops). Pulse growth on `hop_factory_pad.py` is T-471, not this apply.

### Live coverage-set (disk vs PARKED `2026-08-26-org-rsi/APPLIED.md:51–64`)

| set | prior n | live n | notes |
|---|---|---|---|
| `docs/program/*.md` | 17 | **17** | CHARTER PROTOCOL OPS GLOSSARY krpc mods ra-rate RO roster slate tech blocks current desk ship uplink world-model. desk/ship gitignored. |
| `docs/program/tickets/*.md` | 2 | **2** | BRIEF + README. `BOARD.md` **MISSING**. Also `board.jsonl` / `head.json` / `fingerprints.json`. |
| `.grok/agents/*.md` | 16 | **16** | No `spotter.md`. No `walt.md`. Extra vs old census-16-with-spotter is `org-review.md`. All 16 `agents_md: false`. |
| `docs/missions/` (not log bodies) | INDEX + `jebediah/` + `uncrewed/` | same | Historical `jebediah/logs` **442** (228 jsonl + 214 md) unmoved. `uncrewed/logs/` **32** (16 jsonl + 16 md; prior empty). |
| `docs/crew/` portraits | 15 | **15** | `builder.md` **MISSING**. `walt.md` portrait exists; agent card still MISSING. |
| `docs/crew/log/` | 15 | **15** | No `spotter.md`. mortimer `^-` **56** before this closeout line. lars **93**. wernher **55**. gene **106**. |
| `docs/press/` | **9** | **10** | INDEX + STYLE + **8** stories (prior unnamed “7 stories”). |
| `docs/lessons.md` | MISSING | **MISSING** | Parked `docs/archive/2026-08-26-org-rsi/lessons.md` **2872** lines / **156** `##`. |
| `docs/agent-notes.md` | 790 | **791** | Still-true kRPC. |
| `AGENTS.md` | 360 | **368** | Spawn table **10** rows, no Spotter (`AGENTS.md:99–109`, `:213`). |
| `CHARTER.md` | 200 | **200** | Creed intact. `:69` still names `plan.md` fallback; kernel waits (`protocol.py:138–139`). |
| `world-model.md` | 17 | **17** | Chair stub. Dump PARKED. |
| `blocks.md` | 15 | **15** | Gene CLI `pad`/`hop`/`splash`/`tech-unlock` + rails 0. |

Confirmed still **MISSING** (opened; error): `docs/lessons.md`, `docs/program/{lars-rsi,learn-rsi,feedback-plan,plan,briefing,science,vab,note-tech,loop,org-rsi}.md`, `docs/program/org-flow/`, `docs/program/sit-card.json`, `docs/program/tickets/BOARD.md`, `.grok/agents/spotter.md`, `.grok/agents/walt.md`, `docs/crew/builder.md`.

### Coverage gaps vs PARKED dest notes (path/basename never in org-rsi)

Rule: live md in the coverage sets whose **path and basename never appear** in PARKED `docs/archive/2026-08-26-org-rsi/` notes. Same-path content drift is not a gap. Log bodies are not gaps.

**7 files.** All under `docs/press/`. Prior INDEX+STYLE+first-hop **were** named.

| live path | why a gap |
|---|---|
| `docs/press/everybody-left.md` | Basename never in prior notes. Extra story vs prior n=9 (INDEX row 2026-08-26). |
| `docs/press/first-space.md` | Counted only as an unnamed “story”. |
| `docs/press/forest-for-the-trees.md` | APPLIED “Keep press Forest” is a test needle, not this filename. |
| `docs/press/first-fifteen-sci.md` | Basename never. |
| `docs/press/asteroid-xrl-564.md` | Basename never. |
| `docs/press/first-five-sci.md` | Basename never. |
| `docs/press/pad-goo.md` | Basename never. |

Not gaps: `docs/press/INDEX.md`, `STYLE.md`, `first-hop.md`. No extra live md in `docs/program/`, `.grok/agents/`, `docs/missions/` (non-log), portraits, `docs/lessons.md`, `docs/agent-notes.md`, or `AGENTS.md`. No 17th agent card. No 16th portrait. No live `docs/program/` org novel.

---

## 2 What changed (path | action)

Actions as landed on disk this wave. Cite live path.

### Slice 1 — unpin (`983cf46`) owner=wernher

| path | action |
|---|---|
| `tests/test_tickets.py` | REWRITE. Drop `tickets.PRINT` mocks. Stop requiring `render_plan` to copy `cli:` (keep `hop_apo`). Stop requiring basename `live_run` to join `seated_logs_dir()`. Add `uncrewed/logs` is a dir. KEEP packet-omit BOARD/science/vab/blocks/jeb briefing/jsonl (`:1078–1097`). KEEP MISSING novels (`:1544–1566`). KEEP `from_need` shim (`:1099–1107`). |
| `tests/test_desk.py` | REWRITE. Drop `DeskSit(note_tech=)` kwargs; `_sit` fills `note_tech` only if the field exists. `format_sit` still omits `note-tech`. |
| `tests/test_protocol_gate.py` | REWRITE. Same DeskSit pin. KEEP `fly_gate` tickets-only / missing ticket wait. KEEP parse_return `recommended` pins (SCHEMAS drop is T-484). |
| `tests/test_protocol.py` | REWRITE. Add `uncrewed/logs` is a dir (`:54`). KEEP PROTOCOL headings (`:11–34`). KEEP `agents_md: false` glob (`:46–48`). KEEP Mortimer `tickets:` / not `need_builder` (`:50–51`). KEEP seated `jebediah/science.md` `ec_rate` / `recover_banks`. KEEP Forest press. |

Production `.py` untouched this slice.

### Slice 2 — leftover writers (`b992cda`) owner=wernher

| path | action |
|---|---|
| `tickets.py` | REWRITE. Drop `PRINT = …/BOARD.md`. KEEP `infer_links` desk+BRIEF (`:496–497`); org/rsi +OPS (`:522–523`). KEEP `ID_PREFIX`, `attach_run` `who=hank`, `NEED_MAP` shim (`:120–129`). `_tape_jsonl` prefers `telem_run`, else an existing `docs/missions/*/logs` file, never invent a seat (`:445–468`). |
| `uplink.py` | REWRITE. Drop `LOOP_PATH` / `NOTE_TECH_PATH` / `note_tech()` writer. Docstring is seated plan/loop (`:1–16`). `radio_text` is SHIP+UPLINK only (`:332–341`). `_PLAN_META` drops `go`/`recommended`; keeps envelope + leftover `phase`/`next`/`craft` (`:104–113`). |
| `missions.py` | REWRITE. Drop `SHIM_*` / `SCIENCE_PATH`. Docstring is tape id, not one crewed stack. Uncrewed capcom default Walt (`:174`). |
| `main.py` | REWRITE. `vab`/`science`/`note`/`plan`/`review` help print tickets / `docs/missions/<id>/`, not archived twins. KEEP `attach_run(..., who="hank")` (`:106`). KEEP crew-log skip when `commander_for` is none. |
| `docs_inventory.py` | REWRITE. `FORBIDDEN_DISPATCH` + `_ORG_NOVEL_NAMES` add program `plan`/`briefing`/`science`/`vab`/`note-tech`/`loop` and `docs/crew/builder.md` (`:26–46`, `:61–80`). `classify("docs/program/ship.md")` = `live_tape` (`:356–357`). `lesson_headings` parked parser (`:316–323`). |
| `desk.py` | REWRITE. Drop `DeskSit.note_tech`. `format_sit` still omits note-tech. Snapshot file LEAVE. |
| `house_dump.py` | REWRITE. `render_plan` skips ticket twins `go`/`recommended`/`cli`/`campaign`/`science_ids` (`:249–266`). Envelope KEEP. Do not retarget slate goal. |
| `flightlog.py` | REWRITE. Docstring tape-id folder `docs/missions/<id>/logs/` (`:1–8`). Dest still `seated_logs_dir()`. |
| `crew.py` | REWRITE. Default capcom Walt (`:112–116`). |
| `tests/test_tickets.py` `tests/test_missions.py` | REWRITE. Pins match the cuts. |

### Slice 3 — job cards (`89949aa`) owner=mortimer

| path | action |
|---|---|
| `.grok/agents/{hank,lars,gene,wernher,gus,linus,mortimer,pilot,verena,katherine,jebediah,grok}.md` | REWRITE. KEEP `agents_md: false`; first command `tickets packet <Hank-named id>` (Hank’s own first command stays `desk` then `ops next`). KEEP Lars named-helper table (`lars.md:29–38`) + After a miss docstring+`--claim` (`:136–139`). KEEP SCHEMAS fence keys (`lesson:` on Lars Return `:160`; Gene `cli:` aliases `recommended`). Drop Git/RA lectures, VAB T-413–T-420 catalogs, 08-44/10-57/stiff-pbc novels, pytest-as-first-command (pytest after the patch), extra Return keys, `tickets: T-NNN` → `T-/S-/M-/C-NNN`. Hank: hop light is not airborne; do not wait hop stdout; lock live reads `ship.md`; lock live ≠ flying (`hank.md:68–72`). Wernher: lock-live `status` is `kspstuff-read` (`wernher.md:55–58`). Jeb/Grok: drop `phase <plan.phase>`. Verena: `## First command`; Read ≤1 extra (`docs/press/STYLE.md`). |
| `.grok/agents/{bill,bob,valentina,org-review}.md` | KEEP. Thin stubs / historian. Not rewritten. |
| `.grok/agents/spotter.md` `.grok/agents/walt.md` | LEAVE **MISSING**. Do not restore. |

### Slice 4 — switchboard (`022278e`) owner=mortimer

| path | action |
|---|---|
| `AGENTS.md` | REWRITE. KEEP spawn table 10 rows, no Spotter (`:99–109`). KEEP packet desk+BRIEF (`:167–169`). KEEP miss physics docstring+findings (`:8–9`). KEEP Gene-as-merge forbidden (`:178`). Add: hop light stdout is not airborne; parent mid-hop reads `ship.md`; do not wait hop stdout; sit/MET/log disagree → one `stuck-<stem>` PNG then read it; TUI phase start/end/unexpected only (`:76–93`). kRPC traps stay `docs/agent-notes.md` (`:361–364`). |
| `docs/program/PROTOCOL.md` | REWRITE. KEEP `## Handoffs` / `## Parallel` / `## Spawn packet` / `## Return (this job)` (tests pin). Spawn packet already desk+BRIEF (`:364–368`). Lock-live `ops next` is ground batch; off-nominal Gene/Lars/Wernher is parent TUI reading `ship.md` (`:430–433`). Envelope on seated plan; `go`/`cli`/`campaign` on the fly ticket. KEEP `lesson:` kv this apply (T-484). Ends `:597`. |
| `docs/program/OPS.md` | REWRITE. §4.1 lock-live is ground batch; hop light is not airborne; eyes `ship.md`; `status` GET does not write jsonl (`:222–235`). Data-flow `ship.md` radio (`:370–378`). Fly gate tickets-only (`:380–384`). Construction novel §7 **left** (`:435–448`) — leftover, not this NUKE. |
| `docs/program/tickets/BRIEF.md` | REWRITE. Drop stale VAB catalog numbers. KEEP vab-helper door and named helper third path (`:73–82`). Miss physics = docstring + `--claim`. Uncrewed Learn = `attach-run` (`:12–17`). |
| `docs/program/GLOSSARY.md` | REWRITE. `note-tech.md` **MISSING / retired** (`:31`). One hop-light vs `ship.md` row (`:32`). CAPCOM Walt (`:18`). Sit-card Retired (`:38`). Ends `:45`. |
| `README.md` | REWRITE. Dead `docs/program/science.md` href → tickets / `python main.py science-scan` / seated dump (`:80–84`). |

### Slice 5 — sit identity (`4c1654c`) owner=mortimer

| path | action |
|---|---|
| `docs/program/current.md` | REWRITE. Keep `flight: uncrewed` / `pilot: none`. `capcom: Walt Grokman`. |
| `docs/program/roster.md` | REWRITE. Walt CAPCOM (`:10`). Valentina Pilot (stub) (`:18`). Katherine row stays (`:16`). |
| `docs/missions/INDEX.md` | REWRITE. `science.md` is a dump, not a board (`:5`). jebediah Next `logs`. |
| `docs/missions/uncrewed/plan.md` | REWRITE. Envelope only. Twins off. |
| `docs/missions/uncrewed/briefing.md` | REWRITE. Bound dump; go/cli/campaign on the fly ticket. Craft matches desk hs-cone. |
| `docs/missions/jebediah/plan.md` | REWRITE. Historical envelope. T-081 hop kv stripped. |
| `docs/missions/jebediah/briefing.md` | REWRITE. Commander dossier, not the live hop. |
| seated `science.md` | KEEP dump. Do not ARCHIVE (pulse fallback T-486). |
| `docs/missions/jebediah/logs/` | TAPE. **442** unmoved. |

### Verify (`c11e1c3`) owner=wernher

Opened merge tickets (reuse stems; did not reopen T-375/T-378; did not steal T-471). Findings on the work tickets. Kernel also minted RSI twins. See §6.

---

## 3 Injection after

Three layers. Packet is the job. Ticket bus is comms. Markdown only when a ticket cannot hold it.

| Layer | Opens | Does not |
|---|---|---|
| **Packet (cold hire)** | `docs/program/desk.md` + `tickets packet <id>` stdout + `docs/program/tickets/BRIEF.md` (`tickets.py:496–497`; `PROTOCOL.md:364–368`). Org/rsi also skims `OPS.md` (`tickets.py:522–523`). Envelope + findings cap 8. `--deep` opt-in (PNG / craft / last-flight / `telem` CLI, never jsonl rows). Tests pin omit BOARD/science/vab/jeb briefing/jsonl (`tests/test_tickets.py:1078–1097`). | CHARTER, PROTOCOL (Mortimer mutates it; children `agents_md: false`), AGENTS.md, BOARD, jsonl rows, `docs/lessons.md`, dumps, archive, RSI letters, sit-card, spotter, `blocks.md` |
| **Parent / Hank TUI** | CHARTER then PROTOCOL (`AGENTS.md:3`). `python main.py desk`. Seat/slate: `current.md`, `slate.md`, `INDEX.md`. Ticket bus: `OPS.md` + `ops next`. kRPC traps: `docs/agent-notes.md`. Mid-hop: `ship.md`. Stuck PNG when sit/MET/log disagree. | 1 Hz / 15 s swallow; jsonl `read_file`; Gene-as-merge; wait on hop stdout |
| **Job card** | `.grok/agents/<slug>.md` — first command + named helper (Lars) + voice path. 16/16 `agents_md: false` (`tests/test_protocol.py:46–48`). | Second spawn table; `need_*`; lessons append |
| **Hop pid** | Control (`session.py:26` `kspstuff`), jsonl, `ship.md`, last-flight on exit | Second Session; grim press |
| **TUI edges** | Phase start / end / unexpected as Walt | Mid-phase narration |

`FORBIDDEN_DISPATCH` drops archive, lessons, RSI letters, BOARD, sit-card, org-flow, spotter, program shims, `docs/crew/builder.md` (`docs_inventory.py:26–46`). `infer_links` drops forbidden (`tickets.py:486–494`).

Miss physics a cold T-471 Lars actually reads: helper docstring (`hop_factory_pad.py:1–23`) + packet findings (six claims `head.json:18309–18357`) + `tickets feedback --claim`. Not `docs/lessons.md` (MISSING). Lars After a miss: docstring + `--claim` (`lars.md:136–139`); grep `docs/lessons.md` on that card = **0**.

Uncrewed Learn: hop-exit `attach_run(..., who="hank")` (`main.py:106`). `needs_learn` stays false for `campaign=uncrewed`. Do not hire Gene between uncrewed hops.

`ops next` lock-live hires gus/linus/wernher/lars/verena/katherine **only** (`ops.py:216–226`). It does **not** parse `ship.md` or hire Gene. Off-nominal Gene/Lars/Wernher is parent TUI reading `ship.md` (`AGENTS.md:86–91`; `OPS.md:222–231`).

Depth 1. One control writer. Abort officer writes `uplink.md`. `status` is `kspstuff-read` (`session.py:7–11`, `:26–27`).

---

## 4 Tests

Apply ran. Required suite green at verify. f013 / empty-card wait were not weakened. `protocol.py` was **not** edited this wave.

**Verify** (Wernher, `c11e1c3`): `python -m unittest tests.test_tickets tests.test_protocol tests.test_protocol_gate tests.test_desk -q` → **136 tests OK**. Extra: `tests.test_missions` 8 OK; `tests.test_crew` + `tests.test_flightlog` + `tests.test_session` 32 OK (1 skipped).

Pins that still lock the house:

| pin | path |
|---|---|
| Packet omit BOARD / science / vab / jeb briefing / jsonl / blocks | `tests/test_tickets.py:1078–1097` |
| Live MISSING lessons / BOARD / sit-card / lars-rsi / org-flow | `:1544–1548` |
| classify parked_archive for novels even when live MISSING | `:1557–1565` |
| `classify("docs/program/ship.md")` = `live_tape` | `:1566` |
| jebediah plan+science `live_kernel` (blocks ARCHIVE until T-486) | `:1567–1572` |
| PROTOCOL headings + `## Return (this job)` | `tests/test_protocol.py:11–34` |
| `agents_md: false` glob (covers `org-review.md`) | `:46–48` |
| Mortimer `tickets:` present, `need_builder` absent | `:49–51` |
| `jebediah/logs` **and** `uncrewed/logs` are dirs | `:53–54` |
| Forest press, not lessons `Forest is 270` | press tests |
| `format_sit` omits note-tech | `tests/test_desk.py` |
| parse_return `recommended` still required | `tests/test_protocol_gate.py` (unpin is T-484 first) |

This closeout did not patch tests or kernel.

---

## 5 Skeptics

`skeptics_ok=true`. Eight heading skeptics + two post-skeptics. `overnuke=false` on all. `undernuke=true` on leftover-writer / dual-board / tape-basename headings — **scheduled** as tickets, not this apply.

| skeptic | overnuke | undernuke | what it proved |
|---|---|---|---|
| 0 comms-law | false | false | Packet already desk+BRIEF; lock-live ops.py ground-only vs OPS.md TUI lie (slice 4 fixed the doc); PRINT dead (slice 2); SCHEMAS OPEN-TICKET. |
| 1 KEEP/TAPE injection | false | false | Heading did not NUKE. Live novels MISSING. attach_run / ID_PREFIX / hop_factory_pad KEEP. |
| 2 ARCHIVE+NUKE leftover writers | false | **true** | Writers **were** the remaining work; slice 2 landed them. Scheduled leftover after that: `_PLAN_META` still `phase`/`next`/`craft`; science.md pulse fallback T-486. |
| 3 MOVE to tickets | false | **true** | Dual `render_plan` copy **cut** in slice 2. Pulse science.md fallback LEAVE until T-486. PROTOCOL/gene dump-rewrite **cut** in slices 4–5. |
| 4 lessons.md replacement | false | false | Live file MISSING. Replacement is docstring + findings. `lesson:` Return leftover = T-484. Parked novel TAPE not NUKE. |
| 5 uncrewed tape vs kerbal seat | false | **true** | Dest already split. `_tape_jsonl` basename join **cut** in slice 2. 442 logs not MOVE (T-487). Dossier content **cut** in slice 5. |
| 6 job-card cuts | false | false | Slice 3 landed. SCHEMAS drop is T-484, not this burst. Spotter not spawnable. |
| 7 live radio / hop light | false | false | Slice 4 landed hop-light ≠ airborne. Pulse files LEAVE. ship.md kv T-491. |

Post-skeptics (`real:false` = not a restore / not a gate fail):

1. HEAD `c11e1c3`. CHARTER 200 creed intact (Kardashev `:3`, RSI `--claim` `:16–21`, idle pad `:29`, Os founder `:35`). Pulse files not in slice file lists. `session.py` WRITE/READ split. KEEP attach_run / ID_PREFIX / hop_factory_pad / packet desk+BRIEF / T-471 inbox. Spawn table 10 rows no Spotter. Batch Learn **0** in `*.py`. Gene-as-merge forbidden (`AGENTS.md:178`).
2. Gate1 lessons-as-miss-physics **FALSE**. Gate2 spotter spawnable **FALSE**. Gate3 BOARD skim **FALSE**. Gate4 live RSI novels as dispatch **FALSE**.

Crew-log `do_not_touch` leak: `docs/crew/log/{wernher,mortimer}.md` received apply lines (not overnuke of portraits-as-voice). This closeout adds **one** Mortimer line as ordered.

---

## 6 Tickets opened or deferred

This closeout **did not open** tickets. Verify (`c11e1c3`) already filed merge’s list. Reuse stems. Did **not** reopen T-375 / T-378. Did **not** steal T-471.

| id | type | desk | fingerprint | title (short) | status |
|---|---|---|---|---|---|
| **T-471** | control | lars | `rf-ignition-ullage` | pad-RF miss (not this apply) | **inbox** S1 P1 |
| T-484 | systems | wernher | `feedback-return` (4) | Drop SCHEMAS `recommended`/`lesson`/`ask`; unpin `test_protocol_gate` first | inbox |
| T-485 | rsi | wernher | `feedback-return` | RSI ×3 twin | inbox |
| T-486 | control | lars | `fly-science-ids-stale` (2) | Empty bound science aborts; drop seated `science.md` fallback | inbox |
| T-487 | systems | wernher | `thin-tape` (4) | MOVE 442 `jebediah/logs` after `test_telem` unpin | inbox |
| T-488 | rsi | wernher | `thin-tape` | RSI twin | inbox |
| T-489 | control | lars | `control-blocks` (9) | Compose one living rocket; retire `run_factory_vessel`; not T-471 | inbox |
| T-490 | rsi | wernher | `control-blocks` | RSI twin | inbox |
| T-491 | systems | wernher | `telem-eyes-library` (6) | `ship.md` thrust/stage/plume kv only; not jsonl schema | inbox |
| T-492 | rsi | wernher | `telem-eyes-library` | RSI twin | inbox |
| T-493 | systems | wernher | `leftover-prelaunch-ghost` (8) | hangar persist-fail stay Flight; skip-save Tracking is not Close | inbox |
| T-494 | rsi | wernher | `leftover-prelaunch-ghost` | RSI twin | inbox |

Pad occupancy stays **T-471** (`hop_factory_pad.py`). Compose is T-489 later. Do not idle the pad for SCHEMAS / MOVE-logs / ship.md kv.

C-477 remains the capable hang (`inbox`, recover tag). T-081 remains `go: yes` uncrewed `cli: hop`.

---

## 7 Open risks

Named leftovers that still poison the next hire if read as law. Not restored. Not retuned here.

1. **Gitignored sit still lies.** `desk.md:15` `last: … exit=0 abort=none` vs `docs/last-flight.md:1–4` `exit: 2` `abort: pad-dead-no-plume`. Next `python main.py desk` refreshes. Do not Learn from last-flight 40 or from `ship.md`.
2. **`CHARTER.md:69` still `plan.md` fallback.** Kernel tickets-only (`protocol.py:126` `_ = plan`; `:138–139` missing ticket wait). Creed/LEAVE until a creed-legal pass.
3. **`protocol.py` `SCHEMAS` still require Gene `recommended`, Lars `lesson`, Katherine `ask` (`:11–20`).** CHARTER not Return keys (`CHARTER.md:21`). T-484. Lars Return still `lesson:` (`.grok/agents/lars.md:160`). Do not land `from-feedback`.
4. **Pulse still falls back to seated `science.md`** when ticket ids empty (`pad.py:66–81`; `protocol.py:_bound_ids` `:97–108`). T-486. Do not retune pulse while T-471 is inbox.
5. **`_PLAN_META` still preserves `phase`/`next`/`craft`** (`uplink.py:104–113`). `phases.current_phase` still reads seated `phase:`. Dump lag, not a second sit.
6. **`NEED_MAP` / `from_need` shim remains** (`tickets.py:120–129`). Cards must not emit `need_*`. Not the bus.
7. **Immortal `run_factory_vessel`** (`hop_factory.py:278`). Compose T-489. Pad-RF stays `hop_factory_pad.py` (T-471).
8. **442 historical `jebediah/logs` still TAPE.** New writers `uncrewed/logs` (16 hops). T-081 evidence still mixes old jebediah jsonl + uncrewed jsonl. T-487 after `test_telem` unpin. Press links stay.
9. **`slate.md:5–16` bank 2.09 / T-369 / t7-wheel-pbc** vs desk sci 13.6243 / C-477 craft. Goal lines `:3–4` KEEP. Sit body is poison if read instead of desk.
10. **`OPS.md:435–448` construction novel.** Org/rsi packets inject OPS (`tickets.py:522–523`). Kernel in 4.1 is honest; §7 is history.
11. **`.grok/agents/org-review.md` is a 16th card.** Historian only. Not a house spawn. Do not Hank-hire it.
12. **PARKED letters look like law** if a hire `read_file`s `docs/archive/2026-08-26-org-rsi/{lars-rsi,learn-rsi,feedback-plan,lessons}.md`. `FORBIDDEN_DISPATCH` starts with `docs/archive/`. Sit numbers in those letters (sci 2.29, T-465 inbox, `flight: jebediah`) are dead.
13. **T-470 rsi `rf-ignition-ullage` ×8 still inbox** empty of findings except `payload.count`. Pad miss is T-471.
14. **Prefix law:** **0** live `S-` / `M-` ids. **One** live `C-477`. Live T- science/fly stay.
15. **Two apply workflows** still on disk (`org-rsi.rhai` landed; `org-pristine-cutover.rhai` has **no** `docs/archive/2026-08-26-org-pristine/APPLIED.md`). Do not re-fire the unused one against the stale census.
16. **`ship.md` has sit/MET/alt/fuel, not stage/thrust/plume.** T-471 needed a PNG. T-491. Not jsonl schema.

Do not restore: spotter as spawnable, Gene-as-merge, Batch Learn, Return keys as the bus, `need_*` as hire tokens, live `docs/lessons.md` stub, `BOARD.md`, `sit-card.json`, program RSI novels, `docs/crew/builder.md`, `.grok/agents/walt.md`.

---

## 8 This did not retune pulse / CHARTER creed

| wall | still true |
|---|---|
| CHARTER **creed** | Kardashev III (`CHARTER.md:3`). RSI `--claim` door (`:16–21`). Idle pad is a sin (`:29`). Os founder (`:35`). 200 lines. Residual `:69` plan.md fallback LEAVE. |
| Slate **goal** | `slate.md:3–4` bigger rockets, more Δv, farther out. Ad astra. Bank digits stale; goal not retargeted. |
| Pulse law | `hop.py` / `hop_factory.py` / `hop_factory_pad.py` / `physics_warp.py` / `pad.py` / `splash.py` / `science.py` **not** in this wave’s slice file lists. Pad-RF growth vs PARKED 202 lines is **T-471 before this wave** (22 defs, file ends `:557`). Do not raise ignitions. |
| `protocol.py` | Not edited. `fly_gate` tickets-only KEEP. `SCHEMAS` leftover T-484. `_bound_ids` science.md fallback T-486. |
| `ops.py` | Not edited. Lock-live ground-only KEEP (`:216–226`). |
| GameData / `persistent.sfs` | Untouched. Repo GameData MISSING. Never `load persistent`. |
| learn-rsi | `attach_run` `who=hank` KEEP. |
| lars-rsi | `ID_PREFIX` S/M/C KEEP. `hop_factory_pad.py` remains the one pad-RF file. |
| TYPES | 11 unchanged. |
| One control writer | `session.py:7–11`, `:26–27`. |
| Depth 1 | `AGENTS.md:95–96`. |
| Pad occupancy | T-471. An RSI letter does not empty the pad. |
| Live org novel | **No** `docs/program/org-rsi.md`. This report is archive-only. |
| Spotter / Gene-as-merge / Batch Learn / Return-keys bus / `need_*` hire tokens | Not restored. Batch Learn **0** in `*.py`. |

Uncrewed hops already write `docs/missions/uncrewed/logs/`. Historical `jebediah/logs` stay until T-487. Compose-from-Wernher-blocks is T-489 later, not a pulse retune now.

---

## Files read (this closeout)

**PARKED** (said PARKED when opened): `docs/archive/README.md`; `docs/archive/2026-08-26-org-rsi/{README,APPLIED}.md`; parked `lessons.md` headings count **156** / EOF `:2872`; dest listing (novels, org-flow). `docs/archive/2026-08-26-org-pristine/` exists as a **different** harvest — not this index.

**Live sit / law:** `docs/program/{desk,current,ship,slate,CHARTER,PROTOCOL,OPS,GLOSSARY,roster,world-model,blocks,uplink}.md`; `docs/last-flight.md`; `docs/missions/{INDEX,uncrewed/plan,uncrewed/briefing,jebediah/plan,jebediah/briefing}.md`; `docs/press/INDEX.md`; `README.md:70–84`; `AGENTS.md:1–14,:76–109,:165–183,:213,:350–368`.

**Kernel:** `tickets.py` (TYPES, ID_PREFIX, NEED_MAP, `_tape_jsonl`, `infer_links`, `attach_run`); `protocol.py` SCHEMAS + `fly_gate` + `_bound_ids`; `ops.py:216–226`; `desk.py` DeskSit; `uplink.py` radio + `_PLAN_META`; `house_dump.py` `_PLAN_TWINS`; `docs_inventory.py` FORBIDDEN + classify; `main.py:99–106`; `missions.py:172–179`; `crew.py:110–116`; `session.py:1–27`; `flightlog.py:1–8`; `pad.py:66–84` (read, not edited); `hop_factory_pad.py:1–23` + 22 `def`; `hop_factory.py:278` `run_factory_vessel`.

**Tickets:** `head.json` T-081, T-471, C-477, T-484–T-494; `fingerprints.json` stem counts.

**Agents:** list_dir `.grok/agents` (16, no spotter, no walt); `agents_md` grep 16/16; hank/lars/wernher/mortimer first command.

**Tests:** `tests/test_tickets.py:1075–1107,:1535–1572`; `tests/test_protocol.py:1–54`.

**Logs (counts + newest, not whole files):** mortimer `^-` **56**; lars **93**; wernher **55**; gene **106**. Wernher newest is verify `c11e1c3`.

**Git:** `.git/HEAD` `ref: refs/heads/main`; `.git/refs/heads/main` `c11e1c3`; `.git/logs/HEAD:124–129`.

**MISSING confirmed:** live `docs/lessons.md`; program RSI letters + shims + `org-rsi.md` + `org-flow/` + `sit-card.json` + `BOARD.md`; `.grok/agents/{spotter,walt}.md`; `docs/crew/builder.md`.

**Not dumped:** jsonl bodies. **Not edited:** pulse law files, CHARTER creed, slate goal, GameData, `persistent.sfs`, `protocol.py`, `ops.py`, `desk.md` snapshot, `board.jsonl` event shape.
