# Slate

**Recommended:** Hold Wernher — `warp_to_soi` 1× timeout when tts is NaN — then Grok 4761 `python main.py mun --from-orbit` (do not Hangar).

Nobody is debris. Board: `docs/program/rescue.md`.

1. **Grok 4761** — 314×11.8 Mm, still Kerbin. L-030 no high-flyby abort held; then 30 min 1× SOI wait, timeout T+1805. Need apo rails when no Mun patch. Retry `--from-orbit` after the patch.
2. **Grok 4373** — 69×1581 km, peri was in the air (~37 min). That clock ran during 4761's 1× sit. `recover` next if still airborne.
3. **Grok 6189** — 349×354 km parking. TLI after 4373 is safe.

Do not pad-launch. Do not leave a Grok frozen.
