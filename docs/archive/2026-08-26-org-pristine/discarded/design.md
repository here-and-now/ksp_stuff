## Summary
The bus already exists. Packet skim is `desk.md` + `BRIEF.md` (`tickets.py:396-397`). Poison is a **second bus**: `docs/lessons.md` (2872), RSI novels beside PROTOCOL, dual plan/briefing/science, parent `AGENTS.md:3-9` stuffing CHARTER+last-flight+lessons into Hank, uncrewed tape under `docs/missions/jebediah/`. Ticket bus is comms. MD only when a ticket cannot hold it. First slice: archive, unpin tests, stop twins, strip inject. Pad stays hot. Do not undo learn-rsi `attach-run` or lars-rsi S/M/C + pad-RF (`tickets.py:59-63`). Do not retune pulse. Do not rewrite CHARTER creed. This letter is **not** a new `docs/program/*.md`.
## Comms law
Os→Hank (`PROTOCOL.md:20`); Hank `ops next` then those hires (`OPS.md:15-27`). Os-by-name = voice, no spawn (`PROTOCOL.md:22`). Hank leftover + `attach-run`/`landing` (`PROTOCOL.md:8-9,:28`). Gene `go:` on the fly ticket only (`:24`). Mortimer org/RSI/CTT (`OPS.md:20`). Lars pulse / Wernher blocks (`OPS.md:25-26`). **Ticket** = bind/`go`/`capable`/`learn`/finding. **MD** = creed, kernel how-to, Gene render, tape. Crew logs are voice TAPE, not dispatch.
## Injection law
Cold child: job card (`agents_md: false`) + `desk.md` + `tickets packet` + `BRIEF.md` (`PROTOCOL.md:340-345`). `read:` desk+≤2. Control third path = named helper `.py` (`:356-360`). **Forbidden in spawn:** CHARTER, PROTOCOL, OPS, world-model, BOARD, lessons, last-flight, jsonl, archive, `lars-rsi.md`/`learn-rsi.md`/`feedback-plan.md`, sit-card, note-tech, spotter, org-flow. Parent after cutover: `desk.md` + `ops next` + packet + BRIEF — not `AGENTS.md:3-9`.
## KEEP-MD
`desk.md` sit; `tickets/BRIEF.md` how; `CHARTER.md` parent creed; `PROTOCOL.md`/`OPS.md` Hank kernel; `current.md` seat; `slate.md` goal; `GLOSSARY.md` speech; `roster.md` names; `krpc.md` role extra (not packet); `uplink.md` Commander takes; seated `plan.md` Gene render (`protocol fly` fallback one release, `OPS.md:6-8`); seated `briefing.md` Gene→pilot until ticket holds prose; `mods.md`/`tech.md`/`ra-rate.md` disk facts, not spawn.
## TAPE
jsonl, `last-flight.md`, `ship.md`, `docs/crew/log/*`, `docs/flights/index.jsonl`, `note-tech.md`, program `loop.md`, `tickets/{head.json,board.jsonl,fingerprints.json}`. Never inject. Query `telem` / `tickets landing`.
## ARCHIVE
This sit → `docs/archive/2026-08-26-docs-cutover/`: `lars-rsi.md`, `learn-rsi.md`, `feedback-plan.md`, `RO.md`, `org-flow/`, `world-model.md` 800-line dump (leave a 20-line chair stub), `docs/lessons.md`, seated `loop.md`. Not a new live novel.
## NUKE-LIVE
Tests first: `.grok/agents/spotter.md` (`AGENTS.md:106`); `sit-card.json` (`GLOSSARY.md:37`); `tickets/BOARD.md`; `docs/crew/builder.md`; program `plan.md`/`briefing.md`/`science.md` shim writers (`missions.py:192-209`); `desk.py:624` note-tech clip. Stub `blocks.md` to CLI names. Stub `lessons.md` “do not append.”
## MOVE to tickets
`go`/`cli`/`campaign`/`learn` → fly payload; bind → science payload (`PROTOCOL.md:526`); `capable:` → vehicle; open questions → `ops --tag ask`; Practice pitfall → rsi `findings` (stem+count); lessons heading → helper docstring + `close_why`; note-tech mailbox → `type=control --fingerprint`; vab novel → vehicle `C-` (live T- stay). No `need_*`.
## lessons.md replacement
Stop append (`lars.md:165`, `wernher.md:92`, `AGENTS.md:326`, `PROTOCOL.md:564`). Next Lars: named helper + packet `finding:`/`close_why` (`tickets.py:530-546`) + docstring (`hop_factory_pad.py:1-8`). Archive the novel. Drop `Forest is 270` (`test_protocol.py:145-147`) and `## ` requirement (`:197-200`). Stop heading twins (`tickets.py:1666-1692`). `agent-notes.md` = still-true kRPC only.
## Mission identity
CHARTER already `docs/missions/<id>/logs/` (`CHARTER.md:163`). Kernel hardcodes `jebediah` (`tickets.py:406,:424,:458`, `house_dump.py:20-22`) while `current.md:1` seats jebediah on uncrewed. Crewed: `seated_id()`. Uncrewed: `docs/flights/` (`docs/flights/README.md:10`). Keep Jeb dossier for `commander: jebediah`. Do not ticket jsonl schema. Fix `GLOSSARY.md:10`.
## Lars compose
Later, not first slice (pulse-law). Law already (`PROTOCOL.md:139`, `OPS.md:26`). `hop_factory.py` still immortal `:1051`. Pad-RF stays `hop_factory_pad.py`. Reuse `control-blocks` (T-376 done, `head.json:13672`). Wernher catalog = `physics_warp.py` + sit predicates; `blocks.md` stub.
## Job-card cuts
`spotter.md` delete. `hank.md` drop 09-01Z/T-449/session-until essays; keep `desk`+`ops next`. `lars.md`/`wernher.md` drop lessons append. `gene.md` drop world-model dispatch; packet T- and M-. `linus.md` dump not bind. `gus.md` `vab.md` not capable. `pilot.md` note-tech is tape. Floors: Lars **medium** (`AGENTS.md:137`) — fix `PROTOCOL.md:349` low.
## Hank in-between after cutover
`ops next` still. Die: BOARD, lessons-as-facts, last-flight-before-fly, dual program dumps, desk note-tech clip, RSI novels. Live: leftover → desk → attach-run → landing → `ops next`. Uncrewed parent `cli:`. Gene only `needing_go` / off-nominal plan. Depth 1. One control writer.
## Sequence
1. Wernher — unpin `test_protocol.py:145-200`; stop `tickets.py:1666` twins. Pad flies.
2. Wernher — `docs_inventory.py:345` classify; git-mv ARCHIVE set. Pad flies.
3. Mortimer — PROTOCOL/BRIEF/AGENTS inject + 8 cards. No pulse `.py`.
4. Wernher — `infer_links` drop science/vab/blocks novel; kill `sync_shim` (`missions.py:192`).
5. Mortimer+Wernher — NUKE spotter/sit-card/BOARD/builder. Pad flies.
6. Wernher — `desk.py:624` stop note-tech clip.
7. Wernher — uncrewed tape `docs/flights/` / `seated_id()`. Schema untouched.
8. Lars — compose ticket after 1–7. Not this first slice.
## Tickets to open later
1. `type=org` desk=mortimer — apply archive/card cuts (no new `docs/program` novel).
2. `type=systems` desk=wernher fingerprint `control-blocks` — inventory + packet skim + twins.
3. `type=control` desk=lars fingerprint `control-blocks` — one living rocket compose (after 1–7).
4. `type=systems` desk=wernher fingerprint `fly-science-ids-stale` — hop/pad stop parsing seated `science.md`.
5. `type=systems` desk=wernher fingerprint `desk-leftover-vs-krpc` — desk stop clipping `note-tech`.
6. `type=ops` desk=hank `--tag feedback` fingerprint `feedback-return` — parent inject vs CHARTER not-Return-keys.
## Do not touch
CHARTER creed, slate goal, portraits, jsonl schema, `desk.md` snapshot, learn-rsi `attach-run`, `ID_PREFIX` S/M/C, `hop_factory_pad.py`, pulse law this slice, GameData, `persistent.sfs`, TYPE zoo, restore spotter/Gene-merge/Batch Learn/`need_*`/Return keys, pad occupancy, depth 1, one control writer, this plan as live `docs/program/*.md`.