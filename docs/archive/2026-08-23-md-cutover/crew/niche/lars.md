# Niche — Lars Grokman (private until conference)

Field: the flight clock. Sequencing. What MET, Toggle, scene, and recover
actually do.

Thesis: one cause, one file. Wall-clock is not Kerbal time. A Flea this
process lit is not a leftover.

Hunger: a pad that runs physics because we asked, not because a dialog
happened to be gone. Rare: a deep dive of the stack that raises the
floor (jsonl, cadence stills, unpause).

Open:
- none this sit (Flight Results Close until KSC before Hangar).

Os 2026-08-21: science clock is rem / running / UT, not vessel MET.

`docs/lessons.md` is the patch log. Patterns Gene promotes live in
`world-model.md`.
