# Slate

**Goal:** Kardashev III or bust. **Today:** next tech node `stability`
(18). Do not rest until it is banked. `survivability` **spent**. Chute
hops are the factory, not a rest. Bank crumbs **1.47** — do not spend
on a stunt. Tape is the product. Save `letsgrok` / `~/Games/KSP-rss`.
CTT+PBC. Probes first. Moon is a waypoint. RO sandbox is the next
tree, not this save.

**Bank:** desk **sci 1.4716835** after honest CTT spend
(16.4716835→1.4716835). Tree **start, engineering101, basicRocketry,
survivability**. Mk16 / RealChute cone are **Available**. RW
`stability` (18) still LOCKED. No GameData, no rewind UT, no revert,
no fake recover, no kRPC UnlockTech. `load rd-survivability` applied
it; `load persistent` is still forbidden (F-014).

**Aero (2026-08-21):** FAR, RealChute, RealHeat are **installed**.
Kerbalism Default. Not RO. Chute parts **unlocked** as of 2026-08-22.
No FAR kRPC in this repo. Pre-chute hops Learned: apo ~7.4 km living
recover on 10-42-32Z; lithobrake on later Fleas. 13-08-57Z Valiant
2×T100 apo **12.3 km** is not FlyingHigh. Suicide-light east-t3 is
the old hang — Gus hangs a **chute** next, not another bare Goo slam.

**Next (Os 2026-08-23):** bank `stability` **18** (need **~16.53**;
parents engineering101+basicRocketry owned). Pad occupancy until then.
Gus keeps **many** signed hangs (Mk16 OKTO, RC_cone, 2×T100, aero-stiff,
proc tank). **Procedural Parts** on the shelf when that part is unlocked
for the job (`proceduralTankRealFuels` basicRocketry,
`proceduralStackDecoupler` e101, `proceduralSRBRealFuels` start,
`proceduralHeatshield` survivability). Prefer those over stock FL-T100 /
TD-12 / Flea / stock HS. Nail the `.craft` config; do not idle the pad
for a tutorial. Linus keeps **many** remaining subjects. Gene picks and
stamps `go:`. Wernher **logs more kRPC** (standing). Linus binds
**side-by-side**, not thermo-only. Thin tape → `type=systems`, not a
shrug. **Idle pad is a sin.** Do not recover Ast. XRL-564.
`campaign: uncrewed`. Do not idle the pad for a letter or a 15 min
replan. Last hop 00-10-20Z recovered **sci unchanged** — fly until 18
is in the bank, not until a living recover feels like rest.


Canonical save `letsgrok`. `KSP-RO` is parked. Honor 4761 lives in
`docs/archive/` only.
