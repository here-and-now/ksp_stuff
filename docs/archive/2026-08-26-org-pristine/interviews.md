

===== TALK 0 =====
## First command if this were a real hire
`python main.py desk` then `python main.py ops next` (`.grok/agents/hank.md:48–51`; `AGENTS.md:134`; `docs/program/OPS.md:199–213`). Hire **exactly** that print. Packet skim: `desk.md` + `tickets packet T-NNN` + `BRIEF.md` — not `BOARD.md` (`PROTOCOL.md:340–342`). Interview sit: **did not run**.

## Who I actually respond to (name+title)
**Os, Founder** for the loop (`CHARTER.md:35–38`; `PROTOCOL.md:20`). **Mortimer Grokman, CEO** for goal / org RSI / CTT (`PROTOCOL.md:21,38`). Kernel `ops next` is law (`OPS.md:197–199`). Then only named desks: **Gene** (`go` / off-nominal plan, no stick), **Gus / Linus / Lars / Wernher**, **Jebediah** iff `fly: yes` and `commander: jebediah`, **Verena / Katherine** on those types. **Walt** speaks; not a hire. **Spotter** is retired (`.grok/agents/spotter.md:4–11`) — still on disk.

## What Hank does in between that I wait on or re-do
Lock-free leftover first: `recover-probe` / `--recover` / `ksc` (`hank.md:165–171`; `OPS.md:233–244`). After CLI: `desk` → `attach-run` (overwrites uncrewed `payload.learn` `who=hank`) → `landing` → `ops next` (`hank.md:116–131`; `PROTOCOL.md:28,494`; `BRIEF.md:10–15`). Uncrewed: **parent** starts `cli:` (`PROTOCOL.md:25`; T-081 `python main.py hop`, `go: yes`, `campaign: uncrewed` — `head.json:3338–3343`). Nag skipped `tickets feedback`; `from-need` shim (`PROTOCOL.md:494`; `hank.md:178–179`). Lock live: `ship.md` eyes, not jsonl (`PROTOCOL.md:43`). Ground waits on that packet; I re-do desk every conference turn (`AGENTS.md:159–162`).

## Markdown I still write that should be a ticket/stamp/finding
Return fence `ops/hire/packet/pad/why/rsi` (`hank.md:183–192`) vs CHARTER “not Return keys” (`CHARTER.md:19–21`; `PROTOCOL.md:536–539`). `docs/crew/log/hank.md` (16 lines / **12** dated bullets) is not the bus. `from-need` stamps `stack`/`builder`, not the miss class (`hank.md:147–148`). Control-from-`last-flight` novels. RSI letters (`lars-rsi.md`, `learn-rsi.md`, `feedback-plan.md`) still live as MD. **Good path already:** `attach-run` → ticket `learn` (`head.json:3567`).

## Markdown I am still told to READ that is stale or wrong
`AGENTS.md:3–9` injects **CHARTER + PROTOCOL + last-flight + lessons.md + agent-notes + INDEX** into the parent (children `agents_md: false`). `lessons.md` **2872** lines. `BOARD.md:3` 74/471 — BRIEF forbids it. `last-flight.md:1–4` hop **exit 2 abort** vs `desk.md:16` **exit 0** vs T-081 `learn` **pre_launch synth rec=yes** (`head.json:3567`) vs `ship.md:6` pre_launch. `briefing.md:3–17` bank **8.77**, craft **proc-long**, Forest land binds — desk craft **t7-wheel-pbc**, bank **2.2905**, bind T-404/T-460/T-461 (`desk.md:7–26`). `slate.md:5–16` bank **2.09** / T-369. `note-tech.md:1` is **08-21** radio; desk dumps a truncated **08-22** line (`desk.md:27`). `lars-rsi.md:7` still says last-flight exit 0. `world-model.md:506` hop_factory **~779** vs disk **1051**. `missions/INDEX.md`. Spotter card.

## Card rot (Return keys, first command, prefixes, named file)
First command is right. Prefixes S-/M-/C- vs T- are on card (`hank.md:68–69`). Return keys still required. Floors: card+PROTOCOL **Lars low** (`hank.md:54`; `PROTOCOL.md:348–350`) vs AGENTS **Lars medium** (`AGENTS.md:137–138`). Warp: `hank.md:100` “warp is Lars” vs `hank.md:61–63` / `OPS.md:25` **Wernher `physics_warp.py`**. Gene: only `ops next` (`hank.md:74–76`) vs live-watch spawn (`hank.md:108–113`; `PROTOCOL.md:44`). `session.py` reader **already landed** T-454 (`session.py:7–10`) — card still “until then do not run status” (`hank.md:20–23`). Named files: immortal `hop.py` **3300** / `hop_factory.py` **1051** / `hop_factory_pad.py` (T-376). `from-need` + 09-01Z / T-449 essays still in the card.

## Why my last hire was slow (or was not)
Law copied in **AGENTS + CHARTER + PROTOCOL + OPS + BRIEF + hank.md**. Parent cold-start reads a 2872-line lessons novel the children never get. T-081 evidence **114** jsonl (`head.json:3222–3336`); logs dir too large to list (exact n **MISSING**, gitignored). Tape is a three-way lie (last-flight / desk / synth learn). Crew log is Os-radio days, not hops. `briefing.md` and `slate.md` still argue craft/bind/bank. Pad occupancy loses to letters.

## One cheap cut so the next hire is minutes, ticket-native, no lessons novel
Hank reads **`desk.md` + `ops next` stdout + `tickets packet` + BRIEF**. Never open `lessons.md`, `BOARD.md`, CHARTER, `last-flight`, `briefing.md`, `note-tech.md`, `agent-notes.md` on an ops turn. Strip hank.md to first command, floors (one table), leftover/tape CLIs, Return. Move 09-01Z / T-449 / RA / Git / `from-need` onto fingerprints. Uncrewed: leftover clean → parent `cli:` on T-081; Gene stays off the merge bus. Learn is `attach-run`, already.

===== TALK 1 =====
The pad-RF sit is one file; inland is still an immortal `run_factory_vessel`. Miss physics still lands in a 156-heading novel. I answer Hank, not Gene. Ticket bus is the comms; `lessons.md` is the poison.

## First command if this were a real hire
`python main.py tickets inbox --desk lars` then `tickets packet T-NNN` (`.grok/agents/lars.md:72`–`:74`, `docs/program/tickets/BRIEF.md:37`–`:38`). Pytest `-k pad` is pad-RF only — not the inland first command.

## Who I actually respond to (name+title)
**Hank Grokman, COO** hires me (`docs/program/PROTOCOL.md:32`). **Wernher Grokman, CSE** owns the catalog I call (`:33`, `:138`–`:141`; `ops --tag ask --desk wernher --fingerprint control-blocks`). **Mortimer Grokman, CEO** owns RSI/this paper (`CHARTER.md:38`–`:39`). Os talks by name with no spawn (`PROTOCOL.md:22`). Not Gene as merge (`:287`). Not Jeb debrief (`:35`). Not Gus.

## What Hank does in between that I wait on or re-do
Leftover `recover()`+Close (`PROTOCOL.md:27`–`:28`). `desk`, `attach-run`, `landing` (`BRIEF.md:116`–`:117`) — I redo the miss if I cite last-flight (`desk.md:27`). Opens `type=control --fingerprint` (`PROTOCOL.md:35`). Copies `f013` (`:361`). Packet third path = named helper (`BRIEF.md:75`–`:77`) — I redo the hunt if he sends `hop.py` for a factory miss. Pad waits the live `.py` (`PROTOCOL.md:287`). He re-flies; I do not (`lars.md:170`). `from-need` if I emit `need_stack` (`lars.md:85`). I commit; I do not wait him (`BRIEF.md:88`).

## Markdown I still write that should be a ticket/stamp/finding
`docs/lessons.md` `##` (`lars.md:165`; **156** headings, file ends `:2871`). `docs/crew/log/lars.md` **89** bullets (newest `:3` T-469; file last `:91` Flight-Results). `docs/program/blocks.md:1`–`:11` hop-row stamp novel. Return `lesson:` (`lars.md:179`; `PROTOCOL.md:32`, `:483`). CHARTER already made RSI `tickets feedback --claim` (`CHARTER.md:18`–`:21`).

## Markdown I am still told to READ that is stale or wrong
Injected `docs/lessons.md` (`AGENTS.md:9`, `:19`, `:326`). `last-flight.md` (`desk.md:27`). `slate.md:3`–`:16` (Today 08-24, bank **2.09**, Next T-369; live bank `desk.md:7` **2.2905**). `current.md:1` `flight: jebediah` vs uncrewed parent. `note-tech.md:1`–`:5` (08-21; desk snippet 08-22). `docs/missions/jebediah/logs/` **442**. `.grok/agents/spotter.md:3` DEPRECATED. `blocks.md:11` Flea/Hammer/18 km. `docs/crew/lars.md:35` lists `physics_warp.py` as mine. `hop.py:1`–`:30` parked water gospel. `docs/archive/` PARKED.

## Card rot (Return keys, first command, prefixes, named file)
Return still `tickets/stack/lesson/f013/blocks` (`lars.md:176`–`:182`) vs CHARTER no Return keys. First command always `-k pad` (`lars.md:74`). `need_stack` still on the card (`:85`). Prefixes: control stays **T-** (correct; `lars-rsi.md:17`–`:24`). Named file: pad is `hop_factory_pad.py:202`; inland is still `run_factory_vessel` `hop_factory.py:274`–`:1051`; `hop.py` **3301**. `PROTOCOL.md:349` Lars **low** vs card **medium** (`lars.md:14`).

## Why my last hire was slow (or was not)
Cold-start, not the 20-line patch (`lars-rsi.md:10`). Same stem `rf-ignition-ullage` **7×** (`:7`); log **6** pad lines (`lars.md:3`–`:8`). Packet used to be the immortal factory (`lars-rsi.md:10`). Hire-clock **MISSING**. T-465 still inbox after `_pad_hold` (`:49`). House `test_hop.py` **231**. Lessons **2871** + `hop.py` **3301** + `blocks.md` novel before one sit.

## One cheap cut so the next hire is minutes, ticket-native, no lessons novel
Stop injecting `lessons.md`. Miss = `tickets feedback --claim` on the control ticket (`CHARTER.md:18`). Packet = `desk.md` + BRIEF + **named helper only**. Drop Return `lesson:`. First command inbox+packet, not house tests. Compose a living-rocket file from Wernher’s catalog (`physics_warp.py:125`–`:397`); do not grow `hop_factory.py:274` and do not open `hop.py`. Rip the `blocks.md` hop-row into fingerprint stems.

===== TALK 2 =====
Catalog is `physics_warp.py`, not `blocks.md`. Lars already imports a subset. Kernel writes desk/ship/last-flight/BOARD — I still append lessons.

## First command if this were a real hire
`python main.py tickets inbox --desk wernher` then `tickets packet T-NNN` (systems stay `T-`). Packet = `desk.md` + BRIEF + that ticket (`.grok/agents/wernher.md:63-69`). No `world`/`tech`/`parts`. No `--deep` unless tape/PNG.

## Who I actually respond to (name+title)
**Hank Grokman, COO** hires and packets (`PROTOCOL.md:33`). This sit: **Mortimer Grokman, CEO**. Os via Hank. Ground talk by name: **Lars Grokman, VSE** (XOR same `.py`), **Gus Grokman** (`vab-helper`). I do not spawn. Return does not address another desk (`wernher.md:145`).

## What Hank does in between that I wait on or re-do
**Wait:** `desk.py:642` sit; `ops next` ids; leftover n=0 (`desk.md:5`); hop-exit `attach-run`/`landing` Tape (`OPS.md:366-368`); lock (other files ok). **Re-do:** last-flight vs Tape; `hop.py` leftover hunt; `blocks.md` as catalog; `from-need` shim; Gene merge (forbidden `PROTOCOL.md:409-413`).

## Markdown I still write that should be a ticket/stamp/finding
`docs/lessons.md` heading (`wernher.md:92`, `AGENTS.md:326`). `docs/crew/log/wernher.md` (46 bullets). `docs/agent-notes.md` (~790, traps vs novel). Return `ready_to_fly` (`PROTOCOL.md:485`) vs `tickets feedback --claim` (`CHARTER.md:18-21`). Door is already T-378.

## Markdown I am still told to READ that is stale or wrong
`docs/lessons.md` 2872 lines (`OPS.md:369`). `docs/program/blocks.md` — Gene CLI pad/hop/splash, Flea/Hammer/090; **not** sit blocks (`tickets.py:420` injects it as catalog). `note-tech.md` last 08-22 Jeb→Lars (`desk.md:27`). `slate.md` pin vs tickets. `current.md:1` `flight: jebediah` while T-081 `campaign: uncrewed` (`head.json:3342`). `agent-notes` on systems `--deep` (`tickets.py:426`). `.grok/agents/spotter.md` still on disk. `docs/missions/jebediah/logs/` too large to list (uncrewed tape still there). CHARTER+PROTOCOL full vs BRIEF+ticket.

## Card rot (Return keys, first command, prefixes, named file)
First command still `need_qol` / `from-need` (`wernher.md:75`). Fence still `tickets:/ready_to_fly:/files:/blocker:` plus “do not emit `need_*`/`good:`”. Prefixes: T-467 landed (`log/wernher.md:3`). Named file: card still extract leftover from `hop.py` (`wernher.md:89`); `leftover_wreck_before_light` still `hop.py:909` (factory `H.` `:379`); warp sits already `physics_warp.py:7-23`. Card says lock-live `status` still writes jsonl (`wernher.md:57-59`); T-454 closed reader (`log:7`). Frontmatter “Not vehicle control loops” vs body owns blocks.

## Why my last hire was slow (or was not)
Last close **T-467** id-prefix in `tickets.py` (`log:3`) — one kernel patch, same day as T-449/453/454/450/445/442/427/413. Slow is **intake**: CHARTER/PROTOCOL/slate/`lessons.md`/`hop.py` leftover/`blocks.md`, not the diff. Standing hire, not a 20-line sit.

## One cheap cut so the next hire is minutes, ticket-native, no lessons novel
Point packet third path at **`physics_warp.py`**. Stop skim-injecting `blocks.md` (`tickets.py:420`). Catalog = public defs (`physics_warp.py:57-423`, locked `tests/test_physics_warp.py:7-34`). Lars already imports `hop_factory.py:33-41` (`apply_sit_warp`, `chute_arm_sit`/`deploy`, `leftover_call`, `timeout_hit`, `airborne_cannot_pay`, `met_elapsed`); add `want_coast` / `thick_air_*` (now `hop.py:52-63` wrap). Move `leftover_wreck_before_light` into that module. Findings = `tickets feedback`. Drop card `lessons.md` heading + `from-need`. Kernel already writes `desk.md` / `ship.md` / last-flight / `BOARD.md` (`tickets.py:744`) / slate-plan (`house_dump.py:421-435`) — I should not.

===== TALK 3 =====
Log: **106** bullets (`docs/crew/log/gene.md:3–108`); `go:` **94**; `need_*` **25**. Newest is `:3` (2026-08-24). Last 40 are 08-20 pad/geiger (`:69–108`).

## First command if this were a real hire

```
python main.py tickets inbox --desk gene
python main.py tickets packet T-081
python main.py tickets stamp T-081 --field go --value yes --who gene
```

Card mints **M-** (`.grok/agents/gene.md:29–36`). Live fly is **T-081** (`BOARD.md:8`, `head.json:3340–3343` `campaign: uncrewed` `cli: hop`).

## Who I actually respond to (name+title)

**Hank Grokman, COO** — `ops next` hire (`PROTOCOL.md:20–24`, `:44`). **Os, Founder** — almost never (`CHARTER.md:35–40`). This packet: **Mortimer Grokman, CEO**. Ground: **Linus** bind / **Gus** `capable:` onto the fly ticket (`CHARTER.md:123–126`). Commander takes uplink (`PROTOCOL.md:508`). I do not spawn (`gene.md:20`).

## What Hank does in between that I wait on or re-do

Wait: leftover / `hangar: recover|blocked` (`gene.md:39–40`); re-desk after Gus `capable:` (`PROTOCOL.md:309`); `attach-run` Learn overwrite (`BRIEF.md:10–15`). Re-do: I last-write briefing + seated `plan.md` after every stamp (`PROTOCOL.md:500–503`) while Hank already re-flies last `cli:` without me (`PROTOCOL.md:251–256`). Merge-hire after specialists is forbidden (`PROTOCOL.md:409`) and still happened (`feedback-plan.md:11`).

## Markdown I still write that should be a ticket/stamp/finding

`docs/program/briefing.md` + `docs/missions/jebediah/briefing.md` (`CHARTER.md:192`). Seated `plan.md` (`OPS.md:145–146`). `world-model.md` Meaning + Open questions (`PROTOCOL.md:67–72`). Crew log novels (`gene.md` log). Return fence (`gene.md:147–162`).

## Markdown I am still told to READ that is stale or wrong

`docs/program/briefing.md:3–11` — proc-long-pbc, Forest T-077, bank **8.77**. Desk is t7-wheel-pbc / T-404 (`desk.md:12–26`). Twin plans: `expect_apo_max` **140 km** vs **400 km**. `world-model.md:29–51` still T-013 `hop-to-water`. `slate.md:15–16` T-369/T-368 vs desk T-404/460/461. `BOARD.md:8` Forest/Grasslands vs FlyingHigh PresMat. `note-tech.md:65` 08-22 stiff-pbc. CHARTER radio CLI (`CHARTER.md:190`). `lessons.md` not on this card — Practice still copies it (`world-model.md:575–626`).

## Card rot (Return keys, first command, prefixes, named file)

Return `fly/flight/seat/phase/craft/tickets/go/cli/campaign/learn/f013/shot/slate` (`gene.md:147–162`) vs forbid `need_*`/`good:`/`recommended:` (`PROTOCOL.md:430–431`). First command **M-**; live **T-081**. Card ~170 lines of GO folklore (`:50–55`). `hop.py` still named (`:73`). `.grok/agents/spotter.md:1–12` still on disk.

## Why my last hire was slow (or was not)

Last log 08-24 is a conference novel (`gene.md:3`) — stamp + f013 + science_ids + bank + cli. Uncrewed: not my hop (`PROTOCOL.md:251`). Two briefings already drifted (`feedback-plan.md:19`). Open-questions table is 08-21 dispatch (`world-model.md:768–800`).

## One cheap cut so the next hire is minutes, ticket-native, no lessons novel

Stop last-writing briefing, world-model Q table, and crew log. Stamp **T-081** only (`go`/`cli`/`campaign`/`phase`; put `hop_apo` on the fly payload so `plan.md` is not a second live plan). Packet = `desk.md` + `tickets packet T-081` + BRIEF. Finding: `tickets feedback T-081 --claim`.

===== TALK 4 =====
## First command if this were a real hire

`python main.py tickets inbox --desk gus` then `python main.py tickets packet T-387` (live vehicle id). Card’s `C-NNN` + stamp is wrong for this sit: **zero `C-` tickets**; T-387 is still `inbox` with `capable: yes`.

## Who I actually respond to (name+title)

**Hank Grokman, COO** — he hires me with vehicle ids; I stamp `capable` ([PROTOCOL.md:30](docs/program/PROTOCOL.md)). I do not talk to **Lars Grokman, Vehicle Systems Engineer** when he shears a hang. Shear is `far-shear` on a **new vehicle ticket** (or an already-signed alt). Ground ask is `ops --tag ask` + `payload.to` ([BRIEF.md:27](docs/program/tickets/BRIEF.md)), usually **Gene Grokman** (picks hang) or **Wernher** (`vab-helper`). Linus only for `ec_rate × duration_s`. Card “do not tell another desk in Return” ([gus.md:127](.grok/agents/gus.md)) beats [crew/gus.md:20](docs/crew/gus.md) “ask Lars.”

## What Hank does in between that I wait on or re-do

Wait: leftover/KSC, I-014 **re-desk** after `capable: yes` before Linus bind / Gene `go` ([PROTOCOL.md:309](docs/program/PROTOCOL.md)), pad occupancy (I do not Hangar). Re-do: **re-hire + re-stamp** because vehicle tickets never leave `inbox` (T-387 `status: inbox` [head.json:14128](docs/program/tickets/head.json) after `capable: yes`). Hank himself wrote re-stamp `findings` on T-387 (`who: hank` [head.json:14086](docs/program/tickets/head.json)–14116). Tape `attach-run`/`landing` is his, not mine. Git I already commit — I do not wait for him.

## Markdown I still write that should be a ticket/stamp/finding

- [vab.md](docs/program/vab.md) after every stamp ([gus.md:101](.grok/agents/gus.md)) — copy of `payload.craft` / `capable` / do-not-Hangar.
- [docs/crew/log/gus.md](docs/crew/log/gus.md) “one line” ([gus.md:102](.grok/agents/gus.md)) — copy of `tickets feedback --claim`.
- Return fence `capable/craft/f013/tickets/blocker` ([gus.md:114](.grok/agents/gus.md)–122) — CHARTER says findings on the ticket, not Return keys ([CHARTER.md:18](docs/program/CHARTER.md)–21).

`.craft` files stay hardware. Pointer [docs/crew/builder.md](docs/crew/builder.md) is leftover MD.

## Markdown I am still told to READ that is stale or wrong

- [slate.md:3](docs/program/slate.md) “Today 08-24”, bank **2.09**; desk sci **2.2905**. Slate Next T-369/T-368 vs desk bind T-404/T-460/T-461 ([desk.md:26](docs/program/desk.md)).
- [plan.md:13](docs/program/plan.md) `craft: …proc-long-pbc` vs desk hang `t7-wheel-pbc` ([desk.md:12](docs/program/desk.md)).
- [note-tech.md](docs/program/note-tech.md) starts 08-21; desk dumps an 08-22 line ([desk.md:27](docs/program/desk.md)).
- [lessons.md](docs/lessons.md) **2872** lines — parent [AGENTS.md:9](AGENTS.md) injects; **Gus card does not**.
- [vab.md](docs/program/vab.md) as sit object — sit is `desk` hangar/parts/f013.
- [BOARD.md](docs/program/tickets/BOARD.md) — BRIEF: not BOARD.
- [krpc.md](docs/program/krpc.md) RA, [world-model.md](docs/program/world-model.md) I-014 copy.
- [.grok/agents/spotter.md](.grok/agents/spotter.md) still on disk (DEPRECATED).
- Card body itself: T-406 loft / T-413–T-420 catalog / “15 sci spent” vs bank 2.29 ([gus.md:34](.grok/agents/gus.md)–78).
- `docs/missions/jebediah/` **447** files — not a Gus packet.

## Card rot (Return keys, first command, prefixes, named file)

- First command stamps **C-NNN**; live vehicles are **T-** (T-387). Prefix mint exists ([BRIEF.md:7](docs/program/tickets/BRIEF.md)); no `C-` id on disk.
- Return keys still required vs CHARTER “not Return keys.”
- `need_builder` / `from-need` still in card ([gus.md:63](.grok/agents/gus.md)).
- Named file **vab.md** as board ([gus.md:5](.grok/agents/gus.md)); review-own-spawns points at a **53-line** log novel.
- Sit-specific hang list baked into the job card (t7-wheel-pbc, T-406, OX-STAT, T-409).

## Why my last hire was slow (or was not)

Slow because it was a **re-stamp, not a stack**. [log/gus.md:3](docs/crew/log/gus.md)–6: four near-identical `capable: yes` on T-387/T-400/T-406 already signed. T-387 stays inbox → Hank re-hires → I rewrite the vab shelf novel → append another log line. T-405 (helpers) was the one cheap hire; loft was already on disk.

## One cheap cut so the next hire is minutes, ticket-native, no lessons novel

**`status: ready` (or close) a vehicle ticket when `capable=yes`** so `ops next` stops re-hiring the signed shelf. Stop last-writing `vab.md` and the crew log; `payload.craft` + stamp + `tickets feedback --claim` is the board. Strip the loft catalog from `.grok/agents/gus.md` — `desk.md` `hangar:` / `parts:` is sit. First command: inbox, packet **the id Hank named**, stamp only if `capable` is empty. Do not inject `lessons.md`.

===== TALK 5 =====
Bind is ticket payload. Two `science.md` files, slate, horizon, and `desk.bind` still tell three different this-hop stories. I wait on Hank’s desk, not a novel.

## First command if this were a real hire

`python main.py tickets inbox --desk linus` then `python main.py tickets packet T-404` (live T- science; new mints **S-**). Packet is `docs/program/desk.md` + that ticket + `docs/program/tickets/BRIEF.md`. Card also wants `science-scan` / `comms` (`.grok/agents/linus.md:18-34`); desk already has leftover + `f013`.

## Who I actually respond to (name+title)

**Hank Grokman, COO** — hire + ids (`docs/program/PROTOCOL.md:31`). **Mortimer Grokman, CEO** — slate / RSI. After Gus signs, **Gene Grokman, Launch / Flight Director** via payload, not radio (`docs/program/CHARTER.md:123-125`). **Gus Grokman, Vehicle Engineering Lead** — wait `capable: yes` (`PROTOCOL.md:134`). Ground: Gus / Lars. Linus ↛ Commander (`PROTOCOL.md:46`).

## What Hank does in between that I wait on or re-do

Writes `desk.md`; I wait I-014 re-desk after Gus capable (`PROTOCOL.md:309`). After hop: leftover, `attach-run`, `landing` — I rebind from envelope, not last-flight. Routes open science tickets. Does **not** dump MD on stamp (`tickets.py:1903` is opt-in `tickets dump`). `desk.bind` still lists Water T-025/026/028 **plus** PresMat (`desk.md:26`) while dump bound is T-404/460/461 (`docs/program/science.md:22-24`).

## Markdown I still write that should be a ticket/stamp/finding

`docs/program/science.md` last-write (`PROTOCOL.md:478,506`). Seated twin `docs/missions/jebediah/science.md` (`house_dump.py:19-20,423-425`). One log paragraph (`docs/crew/log/linus.md`; **77** bullets; card `.grok/agents/linus.md:96-97`). Horizon in `docs/program/world-model.md:250`. CHARTER door is `tickets feedback --claim` (`CHARTER.md:18-21`).

## Markdown I am still told to READ that is stale or wrong

Packet skim injects `docs/program/science.md` (`tickets.py:414`) — circular dump. slate Next is T-369+T-404+T-368 (`slate.md:15-16`) vs dump trio vs Water on `desk.bind`. Horizon still 15 sci / bank 13.26 / T-020 (`world-model.md:252-259`). `desk.review` last-flight (`desk.md:16`; card forbids Learn). note-tech 08-22 Jeb→Lars (`desk.md:27`). Voice still owns `science.md` / stability 18 (`docs/crew/linus.md:14,30`). Kerbalism cfg gospel (card forbids). `lessons.md` via `AGENTS.md:9` — not on my card (`agents_md: false`). BOARD.md (BRIEF forbids).

## Card rot (Return keys, first command, prefixes, named file)

Return still `science:` / `tickets:` / `f013:` (`.grok/agents/linus.md:105-108`, `PROTOCOL.md:471-475`) vs CHARTER “Not Return keys.” First command packets **S-NNN** while live bind is **T-404**. Prefixes: new S-, live T- stay (`BRIEF.md:5-8`). Named file: program dump vs seated `science.md` still hop/pad fallback (`hop.py:317`, `pad.py:66`). `need_science` / `from-need` still in card (`.grok/agents/linus.md:45`). Sit novel (stiff-pbc 10-57-33Z, T-313/T-288) is a job-card wall.

## Why my last hire was slow (or was not)

2026-08-25 kept PresMat trio after wreck (`docs/crew/log/linus.md:3`) — same paragraph ×3 (lines 3–5). Reconcile dump vs `desk.bind` vs slate vs Horizon. Rewrite dump tables. Job-card sit history ~70 lines. Wait Hank re-desk + Gus capable. `science-scan` even when desk has leftover. Log is the bus.

## One cheap cut so the next hire is minutes, ticket-native, no lessons novel

Stop last-writing both `science.md`. Packet = inbox + ticket payload + desk leftover-science/`f013`. Hop/pad abort on empty tickets — drop seated fallback (`hop.py:328-335`). Strip job-card sit novel and Return keys. Horizon is `tickets list --type science`. Do not inject `lessons.md`. Finding on the work ticket, not a log paragraph.

===== TALK 6 =====
Interview only. Desk lock free, leftover 0, sci 2.2905. Crew log **52** dated bullets (13 on 08-25). `lessons.md` ends **2871**. `hop.py` **3300**, `hop_factory.py` **1051**. `rsi-jump.md` and `tickets/RSI-JUMP.md` are **MISSING**.

## First command if this were a real hire
```
python main.py tickets inbox --desk mortimer
python main.py tickets packet T-NNN
```
Packet is `desk.md` + inbox + this ticket + BRIEF. No `world`/`tech`/`parts`. `.grok/agents/mortimer.md:19`–`:27`. `PROTOCOL.md:340`–`:345`.

## Who I actually respond to (name+title)
**Os, Founder** for objective / CHARTER / RSI org (`PROTOCOL.md:21`, `CHARTER.md:35`–`:39`). **Hank Grokman, COO** hires me lock-free on `type=ctt|org|rsi×3` (`PROTOCOL.md:38`, `OPS.md:74`–`:77`). Linus / Lars / Gene **brief** CTT; I do not dispatch them (`CHARTER.md:88`). Ground talk lock-free by name; I do not spawn (`PROTOCOL.md:59`–`:63`). `need_os` is Os, creed/roster only.

## What Hank does in between that I wait on or re-do
He writes `desk.md` and `ops next` names the T-id. I re-read the novel he stuffed in the packet (this hire: CHARTER+PROTOCOL+slate+log vs `read:` ≤3, `PROTOCOL.md:335`). After CLI he `attach-run`/`landing`; desk still `review: last-flight.md` (`desk.md:16`–`:17`). He shims leftover `need_*` and nags skipped `--claim` (`PROTOCOL.md:494`). Git is mine; last RSI **Git MISSING** so he re-does (`log:3`). He copies **medium**; my card says **high**. Lock live skips org; fly_ready still hires without emptying the pad (`OPS.md:76`–`:77`).

## Markdown I still write that should be a ticket/stamp/finding
Practice novel `world-model.md:322`+ (T-id pitfalls already on tickets). Leftover RSI letters: `lars-rsi.md`, `learn-rsi.md`, `feedback-plan.md`. Crew-log one-liner restating harvest (`log:3`–`:15`). Portrait Notes + slate How-today + CHARTER working-goal copies. Return fence `goal:`/`org:`/`tickets:` (`.grok/agents/mortimer.md:82`–`:90`) vs `CHARTER.md:21` **Not Return keys**.

## Markdown I am still told to READ that is stale or wrong
Parent `AGENTS.md:3`–`:9` injects CHARTER, PROTOCOL, last-flight, `current.md`, slate, **`lessons.md`** (children `agents_md: false`; parent still injects). Slate bank **2.09** vs desk **2.2905**. `learn-rsi.md:13` sit sci **8.7721**. Log `40`–`:42` names **MISSING** `rsi-jump.md`. `note-tech.md` frozen **08-21**. `current.md:1` `flight: jebediah` vs uncrewed. BOARD.md (BRIEF `:4` forbids). Practice still cites `hop.py` ~3298 / `hop_factory` ~779 (`world-model.md:506`). `spotter.md` still a card (`:11` Do not spawn).

## Card rot (Return keys, first command, prefixes, named file)
Return keys still card `:82`–`:90` and `PROTOCOL.md:492`. Reasoning **high** (card `:17`) vs PROTOCOL `:349` / Hank `:55` / portrait `:27` **medium** (Os 08-23). First command T- + S/M/C note is current (lars-rsi applied). Named file is still Practice in `world-model.md`, not `payload.findings`. Portrait “May talk to Gene” (`docs/crew/mortimer.md:18`) vs no Gene merge.

## Why my last hire was slow (or was not)
`lars-rsi` 08-25: apply letter + T-466 harvest **duplicate** (`log:3`–`:4`), Practice last-write, Git MISSING. Prefix patch was small; cold-start re-read the novel I wrote. Pad still flew.

## One cheap cut so the next hire is minutes, ticket-native, no lessons novel
Honor the packet: `desk.md` + BRIEF + `tickets packet T-NNN` only. Practice last-write = one `tickets feedback T-NNN --claim` (stem, count, pitfall). Archive `*-rsi.md`. Drop the Return fence. Reasoning **medium**. Do not inject `lessons.md`.

===== TALK 7 =====
Seat is still `jebediah` while T-081 is uncrewed parent-fly. Tape follows the seat, not the Commander hire. Crew log: 211 lines; `hop exit` 127, `hop exit=0` 70, `ksc leftover` 16. Last 40: [docs/crew/log/jebediah.md](docs/crew/log/jebediah.md) L173–211 (ends 22-06-37Z abort).

## First command if this were a real hire
Refuse. T-081 is `go: yes` `campaign: uncrewed` `cli: python main.py hop` ([head.json](docs/program/tickets/head.json) L3339–3343). OPS: uncrewed → parent starts `cli`, `commander: none` ([OPS.md](docs/program/OPS.md) L252–255). Lock free, leftover 0 ([desk.md](docs/program/desk.md) L1–5). No `cli:` on this interview packet. Inbox-then-CLI is card rot, not a light.

## Who I actually respond to (name+title)
Hank Grokman, COO hires and takes the handoff ([PROTOCOL.md](docs/program/PROTOCOL.md) L26–29). Gene Grokman, Launch / Flight Director stamps `go:` / `cli:` — I copy, I do not rewrite ([jebediah.md](docs/crew/jebediah.md) L37–39). In-flight I take `uplink.md` and `note`/hold/abort; after CLI exit I talk to nobody ([PROTOCOL.md](docs/program/PROTOCOL.md) L41–42, L496). Os via Hank. Linus ↛ me ([PROTOCOL.md](docs/program/PROTOCOL.md) L46).

## What Hank does in between that I wait on or re-do
Leftover `recover()` + Close; `desk`; `attach-run` (overwrites `payload.learn`); `tickets landing`; open `type=control` from last-flight; `ops next`; uncrewed re-run without me ([PROTOCOL.md](docs/program/PROTOCOL.md) L8–10, L28, L36; [OPS.md](docs/program/OPS.md) L247–250). I wait on lock-free leftover 0 + `fly: yes`. I must not re-do leftover/KSC. Desk `last: … exit=0` ([desk.md](docs/program/desk.md) L15) vs [last-flight.md](docs/last-flight.md) L1–4 `exit: 2 abort: abort` — I would re-read the wrong postcard.

## Markdown I still write that should be a ticket/stamp/finding
`python main.py note` → [note-tech.md](docs/program/note-tech.md) (last Jeb 08-22, still on desk L27; writer [uplink.py](uplink.py) L230–241). Return fence `result:/exit:/handoff:/abort:/last:` then `tickets feedback` ([pilot.md](/.grok/agents/pilot.md) L71–87) — CHARTER already made findings the bus ([CHARTER.md](docs/program/CHARTER.md) L16–22). Crew log is a 211-line hop index. During-hop `tickets open --type control` is already the bus.

## Markdown I am still told to READ that is stale or wrong
AGENTS injects [lessons.md](docs/lessons.md) (~2871). [docs/program/briefing.md](docs/program/briefing.md) L3–17: bank **8.7721**, thermo/TELEMETRY, `proc-long`. Seated [missions/jebediah/briefing.md](docs/missions/jebediah/briefing.md) L3–4 is t7-wheel. [plan.md](docs/missions/jebediah/plan.md) L13 craft `proc-long-pbc` vs desk L12 `t7-wheel-pbc` vs T-081 craft `proc-long` (L3218). Slate bank **2.09** ([slate.md](docs/program/slate.md) L6) vs desk **2.2905**. [spotter.md](.grok/agents/spotter.md) L4 DEPRECATED, still on disk. Crew-log reviews → PARKED `docs/archive/reviews/`.

## Card rot (Return keys, first command, prefixes, named file)
Dual cards: [jebediah.md](.grok/agents/jebediah.md) L4 `python main.py phase <plan.phase>` vs live `python main.py hop`. First command: inbox ([pilot.md](.grok/agents/pilot.md) L24–32; [PROTOCOL.md](docs/program/PROTOCOL.md) L345–346) vs “start the exact `cli:`” ([jebediah.md](.grok/agents/jebediah.md) L16–17). Return keys still required ([pilot.md](.grok/agents/pilot.md) L71–79). Floor **medium** on the card vs PROTOCOL **low** (L349). Live fly stays **T-081**, not M- ([BRIEF.md](docs/program/tickets/BRIEF.md) L6–8). Crew style numbers (target 250 km) are not flight ([jebediah.md](docs/crew/jebediah.md) L28–33).

## Why my last hire was slow (or was not)
Last hop 22-06-37Z abort, sit `pre_launch`, apo 85 m ([head.json](docs/program/tickets/head.json) L3544–3567) — parent-fly, not a Commander loop. Hire still reads desk+BRIEF+PROTOCOL+CHARTER+plan+two briefings+note-tech+211-line log+lessons. Packet skim hardcodes `docs/missions/jebediah/briefing.md` ([tickets.py](tickets.py) L406). T-081 evidence is a jsonl wall. Uncrewed seat still names me ([current.md](docs/program/current.md) L1).

## One cheap cut so the next hire is minutes, ticket-native, no lessons novel
Do not hire Jeb when `campaign: uncrewed` (OPS already). Keep `flight: jebediah` only as the tape bucket (`seated_logs_dir` [missions.py](missions.py) L79–104; 442 files). Commander packet = desk + BRIEF + fly-ticket skim — no lessons, no note-tech, no crew log, no program briefing. First command = exact `cli:` or refuse. `attach-run` already owns learn on T-081.