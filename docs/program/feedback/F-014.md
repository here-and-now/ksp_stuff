# F-014 — load persistent autosaves RAM first

from: mortimer
title: load persistent autosaves RAM first
after: 2026-08-20 engineering101
good: SpaceCenter.load of a **named** copy (`rd-engineering101`) applied the node. Disk sci 4.93, geiger UNLOCKED. Os not asked.
bad: `SpaceCenter.load("persistent")` writes the in-memory game onto persistent.sfs before reading disk — the RD spend vanished until a named file was loaded.
suggest: after RD edit, `cp persistent.sfs rd-<node>.sfs` then `python main.py load rd-<node>`. Never `load persistent` for this sit.
status: open
decided:
