# Slate

**Goal:** Kardashev III or bust. Today: Earth science sandbox on
**`letsgrok` / `~/Games/KSP-rss`**. CTT+PBC tree. Probes first. Moon
is a waypoint.

**Aero (2026-08-21):** FAR, RealChute, RealHeat are **installed**.
Kerbalism Default. Not RO. Chute parts LOCKED (survivability 15).
No FAR kRPC in this repo. FAR envelope Learned: apo ~7.4 km, living
recover on 10-42-32Z; lithobrake landed recover on 11-23-25Z,
11-40-22Z, **11-47-13Z**.

**Recommended:** leftover PRELAUNCH after 12-22-36Z abort.
`campaign: none`, `go: yes`, `python main.py phase hop`. Do not
Hangar. sci **6.35** (12-22-36Z +0; 12-04-13Z +0.30 geiger
in-flight). FlyingLow geiger leftover **0.32**. recovery leftover
gone. Start FlyingLow geiger on the part. Not 497 s. Keep Flea
(`kspstuff-hop-flea-pbc`). Hammer-far alt waits. Crash UI detect-now
(frozen MET + flying + q=0 + ~74 m): recover if yes, else Space
Center/Close abort — do not wait landed; do not unpause-spam when
recoverable=no. Living recover: wait landed; `recover()` when
recoverable=yes **before** dismiss. Gus `capable: yes`. Linus bound.
f013 on_craft yes. Do not Hangar `kspstuff-geiger-pbc`. Do not
re-pad. hop_apo 18 km is a cut wish. Never rails.


Canonical save `letsgrok`. `KSP-RO` is parked. Honor 4761 lives in
`docs/archive/` only.
