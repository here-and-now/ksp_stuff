# Improve queue — RSI bus

House friction, not flight physics (`docs/lessons.md`). Anyone already
spawned may return `improve:`. Parent numbers `I-NNN`. **Mortimer
Grokman, CEO** last-writes the house when a trip fires. Gene still
owns `go:`.

## Trip (spawn Mortimer)

Lock **free**, Os not mid-go, and **any**:

- **3+ open** items in this folder
- a return `need_mortimer: org`
- Os says org / retro / RSI
- the same named Practice pitfall repeats on a new sit

Do not hire Mortimer after every pad. Do not hire him to chat.
Queue is memory; the hire is mutation.

## Item

```
# I-NNN — <title>

from: <Name>
status: open | accepted | wont
kind: org | world-model | code | voice
friction: <one line>
suggest: <one line>
code: none | <path>
```

Helm **physics** stays a dated `lessons.md` heading (Lars XOR Wernher).
Helm **house** (packet, desk, spawn tax) lives here.

## Mortimer return

```
org: hold | patched
changed: <paths or none>
need_qol: none | <py or test>
need_os: none | charter | roster
goal: <slate line>
friction_closed: <ids>
```

`need_qol` → parent spawns **Lars** for org code (`desk.py`, waits,
tests) — not pad physics unless the miss was a fly. Wernher iff the
QOL patch is a kRPC trap.

Open F-005–F-015 were queued here at the RSI cut (`I-001`…). Accepted
F-001–F-004 live on world-model **Practice**.
