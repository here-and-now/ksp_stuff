

===== FRICTION 0 =====
## Topic

`docs/lessons.md` is **2872** lines / **156** `##` headings (newest inserted under a 22-line how-to). It is **not** spawn-packet skim. It is still the miss-physics bus: parent AGENTS names it as mission fact; Lars/Wernher must append; tests freeze strings; kernel twins headings into empty `done` tickets. Same fingerprint gets a new heading each miss; older **Fix:** stays. Live law already sits on the helper docstring + ticket `findings`/`close_why`.

## Evidence (path:line)

- Last line `docs/lessons.md:2872`. `## 2026-08-25` **22**. Intro still “After anything unexpected… Append” `:7–12`. Newest `21-57-33Z` `:24`; 08-23 leftover still `:2852`. `lars-rsi.md:7` cites `:24` as 20-36-06Z — that line is now 21-57-33Z (rot).
- `rf-ignition-ullage` headings **6** (`:24 :43 :65 :83 :99 :115`) + MM cfg `:131`. `fingerprints.json:153` count **10**. T-457→T-471. T-465 findings contradict (`head.json:17694` keep independent until MECO vs `:17702` release after thrust). Live: `hop_factory_pad.py:1–8` + `_pad_hold` `:173–201`. T-471 `inbox` `:17897`.
- Other stems: lessons flyinghigh-lid **11** vs fp **16** (`fingerprints.json:40`); hold-ground-card **6** vs **12**; hop-coast **4** vs **7**. Duplicate same-stamp `chute-deploy-sit` `:859 :875`. `Forest is 270` still `:1233 :1248` (08-23 `hop.py` slew).
- **Not packet:** `tickets.py:396–420` control skim = desk + BRIEF + `blocks.md`. `PROTOCOL.md:340–345` `read:` desk + ≤2. `lars.md:11` `agents_md: false`; first command `:72–80` — no lessons path. File only as **deep evidence** if listed (`head.json:360` T-006).
- **Told to use it:** parent `AGENTS.md:9 :19` miss physics / mission facts; R&D append `:326 :345`. `.grok/agents/lars.md:165`; `docs/crew/lars.md:32–33`; `.grok/agents/wernher.md:92`. Keep: `PROTOCOL.md:564`, `OPS.md:369`, `BRIEF.md:83`, `learn-rsi.md:156–159`. CHARTER **0** hits. README `:270`.
- Tests: `test_protocol.py:145–147` whole-file + freeze `Forest is 270`; `:197–200` require `## ` and no `L-NNN`.
- Twins: `tickets.py:1633–1690` one `done` control per heading, `close_why` “patched history; lessons.md is forensics”.
- `agent-notes.md` **791** / 13 `##`. PARKED `docs/archive/kerbin-lessons.md:1–3` still says live agents read `docs/lessons.md`.
- Crew: `docs/crew/log/*.md` **0** `lessons`. `lars.md` log **89** `-` (`:3–91`); last `:91` 14-52-25Z. `rf-ignition-ullage` **6** (`:3–8`).

## Why it happens

learn-rsi **kept** run headings as forensics (`learn-rsi.md:156–159`) while CHARTER already moved learning to `tickets feedback` / `close_why` (`CHARTER.md:16–21`). Append is job-card honor-system, so each miss inserts another dated novel instead of overwriting the helper docstring or one finding. Kernel twins the heading instead of retiring the file. Tests pin leftover prose.

## Damage to the NEXT hire

Cold Lars does not get 2872 lines in packet — then **After a miss** opens the dump (default `read_file` 1000 = newest contradictions). Parent Hank is told mission facts live there (`AGENTS.md:19`), so the TUI may load the novel every conference. Wrong law stays: 10-57 “stage next pulse” (`:110`) and 11-11 “keep throttle 1” (`:94`) still sit above the live pad-RF file. T-471 is the same stem again. Cheapest next-hire surfaces already exist: spawn `read:` named helper (`BRIEF.md:75–80`, `hop_factory_pad.py:1–8`); `tickets packet` prints `finding:` + `close_why` (`tickets.py:504–546`); `agent-notes.md` for still-true API only (`AGENTS.md:345–346`). Stop append; archive `lessons.md`; drop Forest/`L-NNN` freeze; do not mint more forensics twins.

## Files read

`docs/lessons.md` header+rf+end; `AGENTS.md`; `.grok/agents/{lars,wernher}.md`; `docs/crew/lars.md`; `docs/crew/log/lars.md` (counts+`:3–40`); `PROTOCOL.md` packet+564; `OPS.md:360–376`; `BRIEF.md:70–90`; `CHARTER.md:1–80`; `tickets.py` packet/skim/migrate; `hop_factory_pad.py:1–202`; `fingerprints.json`; `head.json` T-006/462/465/469–471; `tests/test_protocol.py:128–200`; `agent-notes.md:1–40 :770–791`; `learn-rsi.md:125–164`; `lars-rsi.md:1–7`; `README.md:270`; PARKED `docs/archive/kerbin-lessons.md:1–12`.

===== FRICTION 1 =====
The live sit is `desk.md` + tickets. Dual MD boards are leftover writers that still get opened: packet injects dumps; Gene/protocol still fall back to seated plan/science; desk clips an 08-22 mailbox.

## Topic
Program vs seated `plan.md` / `briefing.md` / `science.md` / `loop.md`, plus leftover `vab.md`, `note-tech.md`, `world-model` Open questions, `BOARD.md`, `sit-card.json`. Ticket bus is the board; those MD files are shims, dumps, or retired mailboxes. Cold child packet is `desk.md` + `tickets packet` + BRIEF — not CHARTER/PROTOCOL/BOARD.

## Evidence (path:line)
**Sit (live):** `desk.md:1–14` lock free leftover 0 sci **2.2905** craft **t7-wheel-pbc**; `ship.md:5–7` same hang `pre_launch` 22:09Z. Bound `desk.md:18–26` T-404/460/461 + unbound T-025/026/028 mixed on `bind:`.

**plan dual:** shim `docs/program/plan.md:12–16` `expect_apo_max: 140000` craft **proc-long-pbc** `go: yes` **no** `cli`/`campaign`/`science_ids`. Seated `docs/missions/jebediah/plan.md:12–18` `expect_apo_max: 400000` same proc-long, plus `cli: python main.py hop` `campaign: uncrewed` `science_ids: barometerScan,geigerCounter,mysteryGoo`. Canonical is seated (`uplink.py:36–47`; `missions.py:192–209` `sync_shim` copies seated→shim — **not last-run**). `house_dump.py:286–288` plan `craft:` from **fly ticket**; `house_dump.py:345` briefing `craft:` from **desk**.

**briefing dual:** program `briefing.md:3–16` **proc-long**, bank **8.7721**, T-077/T-287 thermo+TELEMETRY. Seated `briefing.md:3–10` **t7-wheel**, T-404/460/461, landing `2026-08-25T10-57-36Z`. Fly skim injects **seated** briefing (`tickets.py:406`).

**science dump vs payload:** bind = ticket payload (`PROTOCOL.md:477–478`; `linus.md:16`). Dumps: program `science.md:19–24` bound T-404/460/461 + 50-row catalog; seated `science.md:1–36` same three dashed rows. Hop/pad still **parse seated science.md** if tickets empty (`hop.py:316–335`; `pad.py:65–81`; `protocol.py:107–118`, `:262–267`). Science skim injects **program** dump (`tickets.py:414`).

**vab vs C-:** `vab.md:2–4` hang **t7-wheel** + alt novel (T-387…T-433). BOARD vehicle = **11 T-ids, 0 C-** (`BOARD.md:10,60–71`). Gus packet `gus.md:42` still `C-NNN`; skim injects `vab.md` (`tickets.py:416`). Seated `craft.md:3` matches desk.

**note-tech:** 65-line 08-21→08-22 Commander mailbox; last `note-tech.md:65` T-081 **stiff-pbc**. `desk.md:27` **clips that line into every packet**. Tape not bus (`GLOSSARY.md:31`; `pilot.md:64`).

**loop vs uplink:** program `loop.md:1` stub; seated `loop.md:1–22` abort/`no_warp` tape. `uplink.md:1` cleared (stick). `note()` writes **seated** loop (`uplink.py:33,50–57`); `main.py:751` help still says program loop.

**world-model vs ask:** Open questions `world-model.md:768–799` last Gene **2026-08-22** hop-to-water; Meaning `:29–47` still T-013 **east-t3**. Dispatch is `ops --tag ask` (`PROTOCOL.md:71–72,103–104`). BOARD **0** `ask` tags.

**BOARD vs inbox:** `BOARD.md:3` open **74/471**. Packet **forbids** it (`PROTOCOL.md:340–342`; `BRIEF.md:3–5`; `gene.md:38`). First command = `tickets inbox`.

**sit-card:** `sit-card.json:2` craft **east-t3-pbc** goo+TELEMETRY. **Retired** (`GLOSSARY.md:37`). Grep hits elsewhere = PARKED archive. Not in packet.

**Who a hire still opens:** every child: `desk.md` (incl. note-tech clip) + packet + BRIEF. Type skim: fly→seated briefing; science→program `science.md`; vehicle→`vab.md`; control→`blocks.md`; rsi/org→OPS. Gene **writes** seated `plan.md` (`gene.md:43–44`); protocol fly **reads** seated plan+science as fallback (`protocol.py:132–135,262–267`). Not packet: sit-card, BOARD, world-model, program plan/briefing/loop, `note-tech.md` whole file, RSI papers. Parent `AGENTS.md:3` still CHARTER→PROTOCOL.

## Why it happens
Ticket cutover left MD writers. `house_dump` dual-writes dumps; `sync_shim` is optional and stale. Plan overlay uses fly-ticket craft; briefing uses desk hang. `protocol fly` / hop/pad keep plan+card so a missing board does not brick (`OPS.md:373–376`). Inbox replaced BOARD; Open questions replaced `ask:`; desk replaced sit-card — files were not deleted.

## Damage to the NEXT hire
Fly packet sees seated briefing (t7-wheel, T-404) — ok — then Gene/protocol can still take seated **plan craft proc-long** vs desk **t7-wheel**. Science hire opens the catalog dump, not bind. Gus opens `vab.md` novel + T- vehicle ids while the card says C-. Every desk sees 08-22 **stiff-pbc** clip. Anyone who wanders: program briefing bank 8.77 / T-077; world-model Meaning T-013 east-t3; sit-card east-t3; BOARD 74-row novel. `ops --tag ask` board is empty; Open questions looks like live conference.

## Files read
`docs/program/{plan,briefing,science,loop,uplink,vab,desk,ship,current,note-tech,CHARTER,PROTOCOL,OPS,GLOSSARY,slate,roster,world-model,sit-card.json}`; `tickets/{BOARD,BRIEF,README}.md`; seated `docs/missions/jebediah/{plan,briefing,science,loop,craft}.md`; `AGENTS.md`; `.grok/agents/{gene,linus,gus,pilot,lars,hank,katherine,wernher,mortimer,verena,jebediah,spotter}.md`; `tickets.py` `house_dump.py` `uplink.py` `missions.py` `protocol.py` `hop.py` `pad.py` `card.py` `ops.py` `main.py`. Archive sit-card = PARKED. Crew logs not opened.

===== FRICTION 2 =====
## Topic
Uncrewed tape still lives under Jebediah. `current.md` seats `jebediah`; parent flies `campaign: uncrewed` with `commander: none`; hop pid still writes `docs/missions/jebediah/logs/`. Mission id = kerbal seat, so the folder is a lie when Hank starts `cli:`. Jeb has not flown in days (`crew=` empty; last named-Commander prose is 2026-08-20).

## Evidence (path:line)
- Seat: `docs/program/current.md:1` `flight: jebediah`. Desk `docs/program/desk.md:6` `seat: jebediah`. INDEX one row `docs/missions/INDEX.md:7`. Tree: `docs/missions/` = INDEX + `jebediah/` only (`list_dir`). Logs **442** files (228 jsonl, 214 md).
- Gate: `tickets.py:927-934` `campaign==uncrewed` → `commander_for` = `"none"`. `PROTOCOL.md:246-250` / `OPS.md:252-254` parent starts `cli:`; **do not hire Jeb**. Seated plan `docs/missions/jebediah/plan.md:16-18` `cli: python main.py hop` / `campaign: uncrewed`. Briefing `docs/missions/jebediah/briefing.md:3` T-081 uncrewed.
- Writer: `flightlog.py:246-269` `dest = seated_logs_dir(seated_id())`; `msg` `command={command} crew={crew}`. `main.py:267` `start("hop", crew="")`. Last hop `docs/missions/jebediah/logs/2026-08-25T22-06-37Z-hop.jsonl:1` `command=hop crew=`. `crew=Jeb` / `crew=[A-Za-z]` = **0**. `command=hop crew=` ≥**100**. `command: phase` in logs = **0**.
- **2026-08-25:** 15 `command: hop` md; 18 jsonl `"kind": "start"` (15 hop + 3 hangar `07-27-09Z` `07-27-52Z` `10-44-16Z`). Index `docs/flights/index.jsonl:131-140` **10** hops, all `"flight": "jebediah"` (file ends `:140`). Five hop stamps on disk not in those 10: `08-20-54Z` `08-40-14Z` `08-48-18Z` `08-56-15Z` `09-01-24Z`. **2026-08-26:** 0.
- Crew log `docs/crew/log/jebediah.md`: **209** `-` lines (`:3-211`). Last 40 start `:173` `2026-08-24T15-24-17Z`. Last `:211` `2026-08-25T22-06-37Z hop exit=2 abort=abort`. Named-Commander only `:8` (2026-08-20 uncrewed Stayputnik). Last crewed Jeb tape = **MISSING**.
- `loop.md` still acks pad aborts `docs/missions/jebediah/loop.md:13-22` (Hank abort T-459/462/465). Program shim `docs/program/loop.md:1` 1-line stub.
- Packet/BRIEF still hardcode the seat: `docs/program/tickets/BRIEF.md:63` attach-run `docs/missions/jebediah/logs/<run>.jsonl`. `tickets.py:406,424,458` skim briefing + live tape under `jebediah/`. `house_dump.py:20-22` PLAN/BRIEFING/SCIENCE hardcoded `docs/missions/jebediah/…` (not `seated_id()`). GLOSSARY `docs/program/GLOSSARY.md:10` **mission** = that folder.

## Why it happens
`missions.py:1-6,79-104` a mission is one **crewed** stack; `seated_id()` is `current.md` `flight:`. Hop never passes a kerbal (`main.py:267`). Uncrewed campaign changed **who starts `cli:`** (`commander: none`) but not **where tape lands**. Gene still renders seated `plan.md` (`gene.md:43-44`, `PROTOCOL.md:500-503`). `sync_shim` (`missions.py:192`) copies onto `docs/program/plan.md` — live shim is already stale (`docs/program/plan.md:12` `expect_apo_max: 140000`, **no** `cli`/`campaign`; seated `:12-18` `400000` + uncrewed). CHARTER still says “Many missions, one Commander” (`CHARTER.md:105`) and “Commander re-fly last `cli:`” (`:171`) while PROTOCOL/OPS say parent + no Jeb.

## Damage to the NEXT hire
Cold packet on a fly ticket still opens **Jeb’s** briefing (`tickets.py:406`) and BRIEF’s attach-run example. Next Gene thinks T-081 is a seated Commander hop; next Jeb (if hired despite `commander: none` — `pilot.md:12` “you **are** `current.md`”) reads 209 log lines of parent probes as his flights. Dual briefing: seated t7-wheel (`briefing.md:4-9`) vs program `proc-long-pbc` bank **8.7721** (`docs/program/briefing.md:3-17`). `loop.md` abort acks look like a live abort officer. Tests pin fixtures to `jebediah/logs/` — moving tape without a seat split breaks them. No `docs/missions/uncrewed/` to seat; INDEX cannot name the parent pid.

## Files read
`docs/program/{current,desk,plan,briefing,loop,CHARTER,PROTOCOL,OPS,GLOSSARY}.md`; `docs/program/tickets/BRIEF.md`; `docs/missions/{INDEX.md,jebediah/{plan,briefing,science,craft,loop}.md}`; last hop jsonl/md; `docs/crew/log/jebediah.md` (counts + last 40); `docs/flights/index.jsonl:131-140`; `missions.py` `tickets.py` `flightlog.py` `protocol.py` `house_dump.py` `uplink.py` `main.py` `crew.py`; `AGENTS.md`; `.grok/agents/{pilot,gene}.md`. `docs/archive/reviews/` hits = **PARKED**.

===== FRICTION 3 =====
## Topic

Lars still grows one inland factory loop. House law says compose **one living rocket** from Wernher blocks (`hop_factory.py` **or** a t7-only file). Applied RSI extracted pad-RF only and **froze compose in `hop_factory.py`**. No t7-only `.py` exists. Wernher “catalog” is `import physics_warp`, not a compose API. Live hang is `kspstuff-hop-valiant-t7-wheel-pbc`.

## Evidence

- Law: `PROTOCOL.md:139-145`, `CHARTER.md:116`, `AGENTS.md:102`, `lars.md:19-22` — t7-only **legal**; immortal Flea/Hammer/4t/splash-090 factory **not**. `lars.md:49` + `lars-rsi.md:3,35,54` + `world-model.md:711-715`: **compose stays `hop_factory.py`**; do not extract lid/chute.
- t7-only / `hop_t7*` / `t7_compose`: **MISSING** (`*.py`).
- Pulse: `hop_factory.py` **1051** (T-376 quoted **~779** `world-model.md:506`). Local helpers **10** + `run_factory_vessel` `:274-1051` (**778**-line `while`). Imports **7** warp sits (`:33-41`); **160** `H.*` sites (~70 hop.py symbols). Slew still `H.WATER_PITCH_UP` `:315`. Helpers stay on hop **so tests patch `hop.*`** (`:19-20`).
- Pad-RF extract: `hop_factory_pad.py` **8** defs `:20-202` (RSI “seven” `:18-169` stale). `hop_factory.py:199-341` pad cluster **MISSING**. Tests: `test_hop_factory.py` **8** (RSI **6**).
- Wernher blocks: `physics_warp.py` **27** defs / **429** lines. `catalog.py:1` is GameData **parts**. `blocks.md:9-12` is Gene CLI names + hop **envelope novel**, not a callable catalog.
- Immortal factory still live: `hop.py` **3300** / **127** defs; inland dispatch `:2300-2314`; water/splash `run_on_vessel` `:2260+`. `pad.py` **771** / **30** defs; `splash.py` **343** / **7**.
- Tests: `test_hop.py` **231** methods, file **~8803** (Wernher card **~7417** stale). `test_source_is_not_a_godfile` (`:442`) only bans `watch`/`pad_pbc`. `test_physics_warp.py` **30**. Packet still “else `hop_factory.py`” (`PROTOCOL.md:356-359`, `BRIEF.md:75-83`, `OPS.md:150`).
- Crew `docs/crew/log/lars.md`: **89** `-` lines. Newest `:3` T-469 pad-RF. Last 40 = `:52-91` (2026-08-21 water/pad). `lessons.md`: **156** `##` headings, miss-append `lars.md:165` (not packet).

## Why it happens

T-376 forbade the immortal factory, then named `hop_factory.py` as the compose. lars-rsi (applied) extracted pad-RF and **banned** lid/chute extract. Packet third path defaults inland to that file. Job card says t7-only is legal **and** “compose stays.” Tests bind `hop.*`, so Lars cannot move helpers without a house-test rewrite. Voice “one helper, stop” adds sits **inside** the same 778-line loop.

## Damage to the NEXT hire

Cold Lars gets a 192-line card (lid/FAR/RF) + packet `hop_factory.py` (1051) + `hop.py` H.* (3300). First pytest is `-k pad` even for inland. Next miss still patches `run_factory_vessel` or `hop.py`; no t7-only file to open; no Wernher catalog besides seven imports. t7-wheel flies water-named constants. `test_hop.py` 231 remains the gravity well. `lessons.md` 156 headings grow on miss; the card injects that duty every hire.

## Files read

`hop_factory.py`, `hop_factory_pad.py`, `hop.py`, `physics_warp.py`, `pad.py`, `splash.py`, `catalog.py`, `tests/test_hop.py` (counts + `:442`), `tests/test_hop_factory.py`, `tests/test_physics_warp.py` (defs), `.grok/agents/{lars,wernher}.md`, `docs/program/{lars-rsi,PROTOCOL,CHARTER,OPS,BRIEF,blocks,world-model,desk}.md`, `AGENTS.md`, `docs/lessons.md` (headings), `docs/crew/log/lars.md`. Archive not opened.

===== FRICTION 4 =====
## Topic
Live job cards still carry a retired spotter, unused pilot stubs, a Gus pointer, and three long pulse-law novels. Prefix, named-helper, and last-flight-as-postcard did not land on every first command. Return fences are parse-kv; findings are a later CLI the kernel does not parse.

## Evidence (path:line)
**Cards on disk (16):** `spotter.md` live, DEPRECATED, 13 lines (`.grok/agents/spotter.md:11–13`). AGENTS table spawn type empty (`AGENTS.md:106`). bill/bob/valentina 12-line `Follow pilot.md` (`.grok/agents/bill.md:11`). grok 18 lines still says `phase` (`grok.md:5`). `docs/crew/{bill,bob,valentina,grok}.md` still have `suicide_start_alt` (`jebediah.md:31`). Logs **1** dash each, 2026-08-20 (`docs/crew/log/bill.md:3`). Walt log **1** (`walt.md:3`); **no** `.grok/agents/walt.md`. `current.md:3` `capcom: Valentina`. `docs/crew/builder.md:1–4` pointer to Gus; live refs only PARKED archive.

**Too long (pulse law copy):** hank **194**, lars **192**, gene **169**. Also wernher 146, gus 127, linus 115. Same warp/lid/RF/tape/09-01Z sermon as `PROTOCOL.md:348–359`, `:375–379`. PROTOCOL/hank: Jeb/Lars **low** (`PROTOCOL.md:349`; `hank.md:54`). AGENTS + tickets README: Lars **medium** (`AGENTS.md:136–138`; `tickets/README.md:36–38`). `lars.md:15` medium.

**S/M/C vs T-081:** gene first cmd `packet M-NNN` but “Live T- fly ids (T-081) stay” (`gene.md:31–37`). gus `C-NNN` (`gus.md:42`). linus `S-NNN` + T-404 (`linus.md:22–27`). Return fences still `fly: T-NNN` (`gene.md:149`; `PROTOCOL.md:448`). hank attach-run `T-NNN` (`hank.md:122`). katherine/verena first cmd `T-NNN` only (`katherine.md:29`; `verena.md:86`).

**Named helper:** table only `lars.md:29–39`, packet third path `:80`. Parent copy `PROTOCOL.md:356–359`, `hank.md:64`, `gene.md:73`. **0** hits in gus/linus/katherine/verena/pilot/stubs.

**Postcard:** last-flight = abort/exit (`PROTOCOL.md:377–379`; `BRIEF.md:42`). Pilot fence still `handoff: docs/last-flight.md` (`pilot.md:76`). gus/verena/stubs: no 09-01Z law.

**Return vs findings:** `SCHEMAS` has **no** findings; gene still requires `recommended` (`protocol.py:12–22`, `:75–76`). Findings door is `tickets feedback --claim` (`PROTOCOL.md:534–548`). Fence forbids `good:` (`PROTOCOL.md:430–432`). After-fence `--claim` on every specialist card; kernel does not parse it.

**Crew logs:** gene **106** dashes (last `:3` T-081 2026-08-24); lars **89** (last `:3` T-469 hop_factory_pad); hank **12**. Flying desks: **0** `tickets feedback` log lines (only mortimer/wernher kernel notes).

## Why it happens
Mortimer applied prefixes/findings/postcard as papers + a comment on some first commands, not a card cut. T-081 is forward-only (do not rename). Pulse law was pasted into hank/lars/gene so children with `agents_md: false` would not miss PROTOCOL. Spotter/builder/stubs were never deleted. Findings stayed off `parse_return` on purpose (`wernher.md:146`) — so the fence is what a cold hire fills, and `--claim` is easy to skip.

## Damage to the NEXT hire
A cold Gene packets **M-NNN** while the live fly id is **T-081**. A cold Lars is **low** in PROTOCOL and **medium** on his own card. A pad miss still needs the parent to name `hop_factory_pad.py`; Gus/Linus never see that table. Pilot still treats last-flight as the product. Findings never gate Return. Spotter remains a spawnable filename. Bill/Bob/Val/Grok still inject suicide knobs. Valentina is still printed as CAPCOM.

## Files read
`.grok/agents/{spotter,bill,bob,valentina,grok,jebediah,hank,lars,gene,pilot,gus,linus,wernher,mortimer,katherine,verena}.md`; `docs/crew/{builder,bill,bob,valentina,grok,jebediah}.md`; `docs/crew/log/{bill,bob,valentina,grok,walt,hank,lars,gene}.md` (counts; gene/lars last lines only); `PROTOCOL.md` spawn/Return/findings; `protocol.py:12–22`; `tickets/BRIEF.md:1–50`; `tickets/README.md:36–38`; `AGENTS.md:94–106,:134–138`; `current.md`; archive `builder.md` hits = PARKED.

===== FRICTION 5 =====
## Topic
Hank **is** the parent switchboard (`OPS.md:44–47`). Kernel already prints who/ids (`ops.py:147–379`). The leftover novel is the **in-between**: spawn packet, dual fly gates, after-CLI tape, last-flight fingerprint, `ship.md` triage, git/screenshot nags, dual MD boards, RSI letters. Children cannot spawn (`AGENTS.md:92`). Ground talk is `ops --tag ask` (`inbox_for` `tickets.py:1405–1428`).

## Evidence (path:line)
**Kernel hire:** leftover→hank (`ops.py:198–217`); lock-live→gus/linus/wernher/lars/verena/katherine **ground only** (`:219–229`, **no** Gene from `ship.md`); S1 recover (`:232`); `go==yes`→uncrewed hank / crewed jebediah (`:243–281`); waste→linus (`:283`); needing_go→gene+batch (`:313`); idle→empty hank hire (`:372–373`), **not** the pad-idle ticket `OPS.md:276–277` claims.

**Still prose:** spawn packet `to/task/read/cli` (`PROTOCOL.md:327–345`) vs `packet_cmd` string only (`tickets.py:550–557`). `protocol fly` uses **plan+card fallback** (`protocol.py:121–135`, `cmd_protocol:264–269`); `ops fly` is occupancy (`ops.py:414–439`). AGENTS lights **`protocol fly`** (`AGENTS.md:173`). After-CLI recipe `desk`/`attach-run`/`landing` (`hank.md:119–125`, `PROTOCOL.md:494`) **duplicates** hop-exit `attach_run(..., who="wernher")` (`main.py:99–106`). Fingerprint from last-flight abort is LLM (`hank.md:134–141`). `from-need` shim (`tickets.py:121–129`, `:560–581`). Lock-live off-nominal hire is table (`OPS.md:284`) **not** `next_actions`. Warp clock (`hank.md:15–18`). Git nag (`OPS.md:35–38`). Parent screenshot (`PROTOCOL.md:313`). `DEFAULT_ROUTE recover=jebediah` (`tickets.py:109`) vs leftover desk **hank** (`OPS.md:155–156`).

**Boards:** desk t7-wheel sci **2.2905** leftover 0 (`desk.md:1–15` `last: exit=0`); last-flight **exit=2 abort** (`docs/last-flight.md:1–3`); ship pre_launch same craft (`ship.md:5–7`); program `plan.md:12` apo **140000** no cli vs seated `plan.md:12–18` apo **400000** `cli: hop`; program briefing **proc-long** bank **8.7721** (`briefing.md:3–17`) vs seated t7-wheel T-404/460/461 (`docs/missions/jebediah/briefing.md:3–10`). `house_dump` writes **seated** plan/briefing + both science dumps (`house_dump.py:17–22`, `:418–432`) — program plan/briefing are **unrendered twins**. `sit-card.json:2` east-t3; GLOSSARY **Retired** (`GLOSSARY.md:37`). `learn-rsi.md:13` sit sci **8.7721**. `lars-rsi.md` / `feedback-plan.md` live reports. Fly packet still skims seated briefing (`tickets.py:405–406`). Parent inject CHARTER→PROTOCOL (`AGENTS.md:3`); child `agents_md: false`, packet desk+BRIEF (`PROTOCOL.md:340–345`). `lessons.md` miss headings (`lars.md:165–167`) — not packet. Crew `hank.md`: **12** `-` lines; last `2026-08-25` Verena/Mortie/warp/RA (`:3–10`).

## Why it happens
Star topology: only parent `spawn_subagent`. Tickets move stamps; **addressing another desk** is extra CLI (`--tag ask`) or Return fence. Desks still dump `note-tech` / world-model table (`world-model.md:790–799` Gene **2026-08-22**) / RSI letters because that is cheaper than a ticket. RSI jumps **write another live md** (`lars-rsi.md:1`). Dual gates keep plan fallback so Hank can light from stale seated kv. Tape is three postcards (last-flight / attach-run / desk `last:`) and Hank **re-derives** the miss.

## Damage to the NEXT hire
Cold **child** gets desk+BRIEF; **parent Hank** still rereads CHARTER 200 + PROTOCOL 566 + OPS 437 + last-flight + dual briefing/plan + RSI papers. Next hop can copy `protocol fly` cli from seated plan **proc-long** while desk is **t7-wheel**. Last-flight abort vs desk exit=0 → wrong leftover vs re-fly. Off-nominal Gene/Lars/Wernher wait on Hank reading `ship.md`, not `ops next`. Glue that **must stay Hank**: leftover `recover()`+Close, one Control writer, depth-1 spawn, KSP screenshot, Os/TUI. Glue that **should be kernel**: one `fly_gate`; stop rendering-or-delete program plan/briefing; one attach-run who; abort→fingerprint ticket; `ship.md` wreck/EC/fuel→hire row; recover desk=hank; RSI papers not live kv; spawn packet fields from the ticket.

## Files read
`ops.py`, `protocol.py`, `tickets.py` (NEED_MAP, packet, attach_run, inbox), `main.py:80–108`, `house_dump.py`, `docs_inventory.py`, `AGENTS.md`, `OPS.md`, `PROTOCOL.md`, `CHARTER.md`, `BRIEF.md`, `hank.md`/`gene.md`/`lars.md`, `desk.md`/`ship.md`/`plan.md`/`briefing.md`/`science.md`/`slate.md`/`current.md`/`loop.md`/`uplink.md`/`sit-card.json`/`GLOSSARY.md`/`lars-rsi.md`/`learn-rsi.md`/`feedback-plan.md`/`world-model.md:790–800`/`note-tech.md:1–8`, seated `plan`/`briefing`, `docs/last-flight.md`, `docs/crew/log/hank.md` (12 lines). Archive hits PARKED, not opened.

===== FRICTION 6 =====
## Topic
Live store is the ticket + `desk.md`. Shims, dumps, novels, and gitignored postcards still sit next to it, and parent/job-card/kernel each open a different set. Packet law is desk + `tickets packet` + BRIEF; that is not what a cold hire actually opens first.

## Evidence (path:line)
**Packet law vs first open**
- Law: `read:` desk + ≤2 role; First command inbox; no BOARD; jsonl = `telem`/`landing`; last-flight `--deep` only (`PROTOCOL.md:327-347`, `BRIEF.md:3-5`).
- Parent (Hank TUI): CHARTER → PROTOCOL → last-flight → desk → current/slate/INDEX → agent-notes + **whole** `lessons.md` (`AGENTS.md:3-9,19`). Children `agents_md: false` (`AGENTS.md:7-8`).
- Kernel skim always desk+BRIEF, then a type file: fly seated briefing, science `science.md`, vehicle `vab.md`, **control `blocks.md`**, org/rsi `OPS.md` (`tickets.py:396-430`). Lars’s named helper `.py` is **not** that third skim path (`lars.md:80-83`).
- Job-card extras (prompt, not `read:`): `krpc.md` (`OPS.md:41-42`); Hank `OPS.md` (`hank.md:78`); Gus `parts --unlocked` (`gus.md:53`); Linus `science-scan` (`linus.md:32`); Verena world-model + crew logs (`verena.md:14,65`); Lars append `lessons.md` (`lars.md:165`); Gene “do not dispatch via world-model” (`gene.md:20`) vs CHARTER chairs it (`CHARTER.md:127-129`).
- `tickets board` still **writes and prints** `BOARD.md` (`tickets.py:16,719-744,1895-1897`; `OPS.md:90`). Open **74/471** (`BOARD.md:3`). Tests keep it out of skim only.

**Wrong store**
- Shim `docs/program/plan.md:12-16` apo **140000** craft **proc-long-pbc** vs seated `docs/missions/jebediah/plan.md:12-18` **400000** + `cli`/`campaign`/`science_ids`. `sync_shim` is copy (`missions.py:192-209`); last copy missed. `protocol fly` still falls back to plan kv (`protocol.py:132-135`).
- Dual briefing: program `briefing.md:4-17` proc-long bank **8.7721** T-077 vs seated `briefing.md:3-10` **t7-wheel** T-404/460/461. AGENTS last-writes **program** briefing (`AGENTS.md:124`); CHARTER seated (`CHARTER.md:192`).
- `sit-card.json:2` **east-t3-pbc**; GLOSSARY **Retired** (`GLOSSARY.md:37`). `science.md` dump still kernel-skim (`tickets.py:414`) while bind is ticket payload (`PROTOCOL.md:478`). Program `loop.md:1` stub vs seated abort tape (`docs/missions/jebediah/loop.md:1-19`). RSI papers / `org-flow/` / `feedback-plan.md` / `spotter.md:11` still live under program (not packet). `house_dump.py:421-431` writes **both** science dumps + seated plan/brief.

**Wrong access**
- last-flight as vessel: “read before flying” (`AGENTS.md:3`); desk `review:` last-flight (`desk.md:16`, `desk.py:322-328`); bank prefers last-flight `sci:` (`desk.py:232-242`). Law: abort/exit (`OPS.md:368`, `BRIEF.md:42`). Live last-flight `exit: 2` abort (`docs/last-flight.md:1-8`) vs desk `last: exit=0` (`desk.md:15`) vs ship pre_launch t7-wheel (`ship.md:5-7`).
- Jsonl: Tape not `read_file` (`PROTOCOL.md:202`, `katherine.md:19`); Linus leftover = “jsonl `sci_rem`” (`linus.md:88`); `--deep` still names the path (`tickets.py:514`).
- `lessons.md` **156** `##`; `classify()` → `live_kernel` (`docs_inventory.py:346`); parent injects the file (`AGENTS.md:9,19`). Packet does not. Miss contract is **one heading** (`lars.md:165`, `PROTOCOL.md:564`). File still demos `python main.py world` (`docs/lessons.md:18`).
- world/tech/parts: “do not re-run if desk this sit” (`CHARTER.md:134-135`, `AGENTS.md:163`, `gene.md:38`). Gus First command still `parts --unlocked` (`gus.md:53`). CHARTER still lists world|tech|parts (`CHARTER.md:70-74`). world-model Facts still `python main.py world` (`world-model.md:21`).
- Crew log: one-line **write** is law; **read as dispatch** (`gus.md:19`, `verena.md:65`). Archive: packet filters `docs/archive/` (`docs_inventory.py:27-31`) but cards **name** `kerbin-lessons.md` (`docs/lessons.md:4`, `verena.md:65`) — PARKED bait.
- world-model Meaning still T-013 10-11-27Z (`world-model.md:29-40`); last Gene **2026-08-22** (`:799`). note-tech last Jeb **2026-08-22** (`note-tech.md:65`) still quoted on desk (`desk.md:27`).

## Why it happens
Shims kept “so old readers match” (`missions.py:193`) and never deleted. Kernel still prints a human BOARD. Job cards ate 09-01Z novels + role extras because skim felt thin. Parent `AGENTS.md` is a third bible. `classify()` treats lessons as kernel. Gene’s radio path in AGENTS still points at the shim briefing.

## Damage to the NEXT hire
Cold child opens the job-card novel + desk + BRIEF + a type MD, not CHARTER — then hits twins (proc-long vs t7-wheel vs east-t3; apo 140 vs 400 km; bank 8.77 vs 2.29; last-flight abort vs desk exit 0). Gus re-runs `parts`. Lars opens `blocks.md`, not the named `.py`. Parent eats 156 lesson headings + 800-line world-model + CHARTER/PROTOCOL. One AGENTS line still Learn-from-postcard. Pad work starts from the wrong craft/bind.

## Files read
`docs/program/{PROTOCOL,CHARTER,OPS,BRIEF,desk,plan,briefing,science,loop,slate,GLOSSARY,sit-card.json,BOARD.md,world-model,note-tech,ship,current}.md`; seated `docs/missions/jebediah/{plan,briefing,science,loop}.md`; `docs/last-flight.md`; `docs/lessons.md` (headings); `AGENTS.md`; `.grok/agents/{gene,hank,lars,linus,gus,wernher,mortimer,pilot,verena,katherine,jebediah,spotter}.md`; `tickets.py` packet/BOARD; `desk.py`; `missions.py`; `protocol.py`; `house_dump.py`; `docs_inventory.py`; `uplink.py`; `main.py` brief. Archive grep = PARKED. Crew logs: not opened (card refs only).

===== FRICTION 7 =====
## Topic
Live RSI letters + `org-flow/` sit beside CHARTER. Inventory classifies the three mds as **live_kernel**, not leftover. Cold packet does not name them. Next Mortimer/Hank still will.

## Evidence (path:line)
- Tree: `lars-rsi.md` 54, `learn-rsi.md` 186, `feedback-plan.md` 47, `org-flow/` (`index.html` + 6 svg, 0 md). Live `rsi-jump.md` = **MISSING**. PARKED: `docs/archive/2026-08-23-md-cutover/program/rsi-jump.md:1` (“RSI jump (applied)”, sit sci **1.47**).
- `_ORG_NOVEL_NAMES` `docs_inventory.py:47-59` = SPEED/ORG/NEXT-ORG/rsi-jump/ticket-bus-cutover — **not** these three. `classify` fallthrough `:346` → **live_kernel**. html/svg not walked (`:362`).
- Workflows **write** the pile: `.grok/workflows/lars-rsi.rhai:356`, `learn-rsi.rhai:573`, `feedback-plan.rhai:11`.
- Cold packet: `PROTOCOL.md:340-345` desk+packet+BRIEF; `gene.md:35-36`; `agents_md: false`. Parent `AGENTS.md:3-6` CHARTER→PROTOCOL. Job cards: **0** hits on those paths.
- Only live-md law cite besides the log: `world-model.md:464` “Applied: `learn-rsi.md`”.
- Competing door: `learn-rsi.md:92` `--good --self --them`; `CHARTER.md:19` / `PROTOCOL.md:534-548` / `tickets.py:1750` `--claim`; `feedback-plan.md:1` “compile, no apply”; mortimer `:19` applied vs `:20` compile-only.
- Stale sit: `learn-rsi.md:13` sci **8.7721**; `feedback-plan.md:7` t7-**chute**; `desk.md:7-12` sci **2.2905** t7-**wheel**. `lars-rsi.md:7` matches desk.
- `lars-rsi.md:54` “Git commit **MISSING**”. Leftover open cmd still `learn-rsi.md:151`; fp `payload-learn-attach` count **1**.
- `org-flow/index.html:22` / `06-rsi.svg:11`: RSI is a fingerprint, not a paragraph. **0** tracked-md inbound.
- Cutover PARKED old novels (`docs/archive/2026-08-23-md-cutover/README.md:12`). New letters never parked.
- Crew: mortimer.md **52** `-` lines (`:3-54`). Cites lars-rsi `:3`, feedback-plan `:20`, learn-rsi `:27`, live `rsi-jump.md` path `:40` (file gone).

## Why it happens
Each RSI Report phase last-writes `docs/program/<jump>.md`. 08-23 cutover parked the *previous* names into `_ORG_NOVEL_NAMES`; new names were not added. Law is copied into CHARTER/PROTOCOL/BRIEF; the letter is not deleted.

## Damage to the NEXT hire
Mortimer/Hank `list_dir` program/ sees three kernel-class novels + unlinked `org-flow/` next to CHARTER. Practice points at learn-rsi. They re-read 186 lines of 8.77-sci sit and a trio door the kernel already dropped. Cold children are safe (packet). Org hire is not. Next RSI adds another live md.

## Files read
`docs/program/` listing; `lars-rsi.md` `learn-rsi.md` `feedback-plan.md`; `org-flow/index.html` `06-rsi.svg`; `docs_inventory.py:47-59,:307-346`; `CHARTER.md:1-80,:110-199`; `PROTOCOL.md:320-365,:498-565`; `OPS.md:1-50,:370-389`; `GLOSSARY.md:1-41`; `BRIEF.md:1-20`; `desk.md:1-20`; `world-model.md:450-470,:700-720,:790-800`; `sit-card.json:1-15`; `AGENTS.md:1-10`; `.grok/agents/{mortimer,hank,gene,lars}.md`; `tickets.py:1745-1764`; workflows `lars-rsi`/`learn-rsi`/`feedback-plan` Report; `docs/crew/log/mortimer.md` (52 `-`); PARKED cutover README + `rsi-jump.md:1-15`.