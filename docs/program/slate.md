# Slate

**Goal:** Kardashev III or bust. Today: Earth science sandbox on
**`letsgrok` / `~/Games/KSP-rss`**. CTT+PBC tree. Probes first. Moon
is a waypoint.

**Aero (2026-08-21):** FAR, RealChute, RealHeat are **installed**.
Kerbalism Default. Not RO. Chute parts LOCKED (survivability 15).
No FAR kRPC in this repo. FAR envelope Learned: apo ~7.4 km, living
recover on 10-42-32Z; lithobrake landed recover on 11-23-25Z.

**Recommended:** `python main.py hop`. sci **5.33** (11-40-22Z +0.54;
recover landed before dismiss). KSC empty. hangar **none**. FlyingLow
geiger leftover **1.40**. recovery crumbs **0.028** skip. Start
FlyingLow geiger on the part. Not 497 s. Wait landed in Flight;
`recover()` when recoverable=yes **before** dismiss. Do not dismiss
a flying wreck. Gus `capable: yes`. Linus bound. f013 on_craft yes.
Do not Hangar `kspstuff-geiger-pbc`. Do not re-pad. hop_apo 18 km
is a cut wish. Never rails.
go: wait.

Canonical save `letsgrok`. `KSP-RO` is parked. Honor 4761 lives in
`docs/archive/` only.
