# Campaign backups

`2026-08-19-mun-campaign.tar.gz` is local (gitignored): missions, flights,
program boards, crew logs from the first Mun attempt (4761 honor).

`letsgrok-2026-08-21/` is the Earth gym (KSP-rss, Kerbalism Default, PBC
pad/hop). RO house does not ingest it.

`2026-08-23-md-cutover/` is leftover nested markdown (gym I/F, niche,
org novels, review-md). Parked — not dispatch. Ticket twins are the bus.

Agents, kRPC traps (`docs/agent-notes.md`), and `.py` stay in the tree.
