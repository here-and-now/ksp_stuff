# Building blocks — Gene may only name these

Owned by **Lars Grokman, Vehicle Engineering**. Empty until a stack
exists on KSP-RO. Gym pad/hop/splash are archived
(`docs/archive/letsgrok-2026-08-21/program/blocks.md`). Do not `go:`
a name that is not in this table.

| Phase | CLI | Expect |
|---|---|---|
| tech-unlock | `python main.py tech-unlock [node]` | Ground. kRPC 0.6 has no UnlockTech. RO sandbox: do not invent a CTT spend. |

Missing name → `need_stack`. No heredocs.
