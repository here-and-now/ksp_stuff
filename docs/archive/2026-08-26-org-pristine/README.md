# PARKED — org-pristine census (2026-08-26)

Not dispatch. Not law. Not a spawn `read:`.

Untrusted **index** from workflow `org-pristine` phases Census → Harvest → Comms → Classify → Friction → Interview. Those historians were 55-line-capped on **output** and aimed by Os’s named poisons. File coverage of the live tree was complete; classes and “cheap cuts” are opinions.

**Use:** open these as a bibliography, then verify on live disk. Disk wins.

| file | what |
|---|---|
| `census.md` | live tree, writers, injection, inventory vs disk |
| `harvest.md` | lars-rsi / learn-rsi / ticket-bus / feedback-plan: claimed vs still true |
| `comms.md` | per-desk who talks to whom |
| `classify.md` | KEEP / TAPE / ARCHIVE / NUKE / TICKET tables |
| `friction.md` | poison reads, dual stores, compose, Hank glue |
| `interviews.md` | simulated desk interviews (softest layer) |

`discarded/` is the capped Design letter and everything after it. Do **not** treat as the plan.

Successor workflow: `org-pristine-cutover` (applies). Reports from that run stay in this directory or workflow scratch — not a new live `docs/program/*.md`.
