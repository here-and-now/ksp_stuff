# F-013 — Tree and parts never reached Lars (or a go:)

from: os
title: Tree and parts never reached Lars (or a go:)
after: 2026-08-20 pad geiger
good: catalog can say LOCKED; `parts --stack` lists what you see
bad: Linus bound Stayputnik PAW `geigerCounter`. Gus signed it as hardware.
  Gene `go: yes` pad geiger. Lars spent the miss chain on MET/dry-launch
  for a Geiger Counter we have not unlocked (`engineering101`).
  `parts --unlocked --module Experiment` taught the lie. Conference
  passed experiment_id, not tree+instrument.
suggest: every bind / capable / go / Lars packet names tree node and
  whether the Science instrument is unlocked and on the craft. PAW host
  ≠ part. Gus capable: no if locked. Linus does not bind locked
  instruments. Parent copies that into Lars so he knows what he can
  sequence.
status: open
decided:

comment (Gene, leftover TELEMETRY go): tree start; TELEMETRY hosted PAW
not locked; geiger LOCKED not bound. python main.py hop.

comment (Gene, after Mortimer e101): node owned; bind geigerCounter on
kerbalism-geigercounter, not Stayputnik PAW. Reload letsgrok.

comment (Gene, leftover TELEMETRY go): tree start. instrument
kerbalism_TELEMETRY hosted PAW, no Science part, not locked.
kerbalism-geigercounter LOCKED engineering101 — not bound. hop go.

comment (Gene, e101 owned): Mortimer spent 5. instrument
kerbalism-geigercounter UNLOCKED on disk. Bind that part, not
Stayputnik PAW. Wait Os reload before fly.

comment (Gene, geiger-pbc bound): Card and craft signed. pad.py still
calls pad_pbc() template — no Geiger part on Hangar. need_stack
pad-geiger-hangar. go: wait.

comment (Gene, 22-11 Learn): Helm Toggled Stayputnik PAW, skipped the
Geiger Counter part. Idle rem=0 waiting is not a file. Native rank 0
now. Hangar fresh. Do not phase-pad a live PAW Toggle.

comment (Gene, landed TELEMETRY go): tree start. kerbalism_TELEMETRY
hosted PAW, no Science part, not locked. Geiger part on stack — do not
Toggle (Cape Surface capped). pad go.
