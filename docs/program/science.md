# Linus board — science program

Query after desk. Do not copy gym Cape / PBC / geiger from memory.

```bash
python main.py desk
python main.py science-scan
python main.py parts --unlocked
```

Bind only after Gus `capable: yes`. Schema: `experiment_id`, `part`,
instrument + tech + unlocked + on_craft (`f013`), `duration_s`,
`ec_rate`, `recover_banks`.

science: none
