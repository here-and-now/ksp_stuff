# World model — House Grokman

Gene Grokman, Flight Director chairs **flight** layers. Mortimer chairs
**Practice**. Others propose. Canonical only after the chair merges.

| Layer | Owner | Source of truth |
|---|---|---|
| **Facts** | disk | `docs/program/desk.md` |
| **Meaning** | Gene | Learn on the named review |
| **Horizon** | Linus | remaining subjects |
| **Story** | Verena | `docs/press/`, README — never invent orbit |
| **Practice** | Mortimer | pitfalls, house, QOL |

Kardashev III is creed here. Joke in the TUI. Nobody preaches mid-burn.

Gym Facts (Cape geiger, Flea, XRL-564, sci 2.43) live in
`docs/archive/letsgrok-2026-08-21/program/world-model.md`. Do not copy
them onto this file.

---

## Facts (disk)

No RO `letsgrok` save yet. Seat `KSPSTUFF_KSP=$HOME/Games/KSP-RO` only
after first boot. See `docs/program/RO.md`.

## Meaning (Gene)

Empty until first Learn on RO.

## Horizon (Linus)

Empty until science-scan on RO.

## Story (Verena)

Empty until a clean first.

## Practice (Mortimer)

Still true as ops (portable from the gym):

- `desk.md` is the sit. Children do not re-run `world`/`tech`/`parts`
  if that file is this sit.
- `hangar:` is the Hangar call (`none` / `recover <name>` / `blocked`).
  Disk cannot see crash UI (`scene: unknown`).
- `f013` (instrument, tech, unlocked, on_craft, host) on every bind /
  capable / `go:` / miss. Missing line = wait. PAW host ≠ instrument.
- `agents_md: false`. Job card + desk.md. Portrait logs:
  `docs/crew/log/`.
- Gene max two hires per sit. Lars miss only. Wernher iff `stack: ok`
  and a kRPC trap.
- `python main.py load rd-<node>` never `load persistent`. After load,
  `ksc` if Flight is an asteroid. Do not recover the rock.
- One kRPC writer. No rewind UT. Crash UI is leftover-or-Hangar.
- Os asks mid-flight → parent reads `ship.md`. Wreck → Walt + one PNG.
- `improve:` files `I-NNN`. Mortimer mutates the house; Gene mutates
  the fly.

## Open questions (between exits)

Parent files `ask:` here. One reply wave if it blocks `go:`.

| from | to | q | status |
|---|---|---|---|
| — | — | — | — |
