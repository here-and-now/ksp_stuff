# Slate

**Goal:** Kardashev III or bust. Moon is a waypoint. Probes first.
**Today (Os 2026-08-24):** bigger rockets, more Δv, farther out. Ad
astra. `stability` **spent**. Next CTT is `generalRocketry` **20**.
Bank crumbs **0.19** do not pay 20. Do not spend crumbs. Farther hops
that can bank FlyingHigh / a new biome are the work. Tape is the
product. Save `letsgrok` / `~/Games/KSP-rss`. CTT+PBC. RO sandbox is
the next tree, not this save.

**Bank:** desk **sci 1.4896**. Tree **start,engineering101,basicRocketry,survivability,stability**. `stability` (18) LOCKED — need ~**16.51**. `load persistent` is forbidden (F-014).
**Aero (2026-08-21):** FAR, RealChute, RealHeat **installed**. Kerbalism
Default. Not RO. Chute parts **unlocked**. No FAR kRPC in this repo.

**Next:** Splash this-hop is **T-313** thermo + **T-288** TELEMETRY leftover
(`SrfSplashed@Forest`) on stiff-pbc — that hang **splashes**. Forest
SrfLanded leftover (T-077/T-287) waits a hang that lands. FlyingHigh
Forest leftover **1.51** waits **≥50 km**. Gus **T-318** hangs more Δv
than stiff (~30 km apo). Living +0: **08-44-03Z** Shores land 7 m/s
apo 831 m; **10-57-33Z** Forest splash 5 m/s unstarted splash thermo.
Do not re-fly those binds. Next CTT is `generalRocketry` **20**
(`sasModule` / PresMat Available; do not spend crumbs **0.19**).
`campaign: uncrewed`.

**How today:** pad occupancy on a hang that can **pay** the last-envelope
sit/biome, not another living +0. FlyingHigh leftover is **t7-chute**
≥50 km (88.8 km proof), not silk / abort-at-lid / a 30 km stiff /
wait-at-800 m then pitch (17-50-46Z apo 782 m).
Do **not** Hangar 4t / dv5 / girderless lite (`far-shear`). Idle pad is
a sin; a living recover that cannot pay is waste. Chute-late lithobrake
is T-339 (`chute-deploy-sit`), not the splash bind. Warp the coast. Thin
tape → `type=systems`. Do not recover Ast. XRL-564. Do not idle the pad
for a letter or a 15 min replan.

Canonical save `letsgrok`. `KSP-RO` is parked. Honor 4761 lives in
`docs/archive/` only.
