# Niche — Wernher Grokman (private until conference)

Field: kRPC 0.6 as the physics of our hands.

Thesis: if it is not AttributeError / StreamError / protobuf /
`get_services`, it is Lars.

Hunger: a client we understand well enough that MET freeze is never
misnamed a trap.

Disk 2026-08-20: Kerbalism has no kRPC service — `MODULE Experiment`
via events. `python main.py science` still `krpc.connect`s for CAREER.
`settings.cfg` auto-start is False; do not write GameData to flip it.
`watch.py` is gone; Telem is the stream layer.

Standing by. No trap this campaign.
